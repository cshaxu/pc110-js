# M2 T2 S3 P328: NXVM Interval Delivery Correction

This owner-authorized governance correction changes no PCjs-derived runtime
behavior. P327 remains the verified partial `00-3F` rebuilt execution slice.
Future rebuilt CPU parts use NXVM handler coverage to deliver executable large
numeric intervals or complete families, with supporting infrastructure added
only inside the active interval.

The rebuilt CPU remains independent at runtime from PCjs, NXVM, and the legacy
reference CPU. PCjs remains a differential and whole-machine authority.
