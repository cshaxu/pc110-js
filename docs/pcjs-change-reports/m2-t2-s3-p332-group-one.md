# M2 T2 S3 P332: Rebuilt Group One

The rebuilt CPU now executes every `80/81/83` immediate arithmetic extension,
including byte, word, dword, ModR/M memory, and `83` sign extension.

No PCjs runtime code changes. PCjs remains a differential and whole-machine
comparison authority.
