# M2 T2 S3 P341: Rebuilt Moffs MOV

The rebuilt CPU now executes all `A0-A3` accumulator moffs MOV forms with
operand/address-size prefixes and segment overrides.

No PCjs runtime code changes. PCjs remains a differential and whole-machine
comparison authority.
