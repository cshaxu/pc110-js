# M2 T2 S3 P346: Rebuilt LOOP Family

The rebuilt CPU now executes E0-E3 LOOP, LOOPE, LOOPNE, and JCXZ with
address-size-selected CX/ECX behavior.

No PCjs runtime code changes. PCjs remains a differential and whole-machine
comparison authority.
