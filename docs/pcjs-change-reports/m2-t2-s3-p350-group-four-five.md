# M2 T2 S3 P350: Rebuilt Group Four/Five

The rebuilt CPU now executes FE/FF local INC, DEC, near CALL, near JMP, and
PUSH forms through project-owned state, memory, and stack boundaries.

No PCjs runtime code changes. PCjs remains a differential and whole-machine
comparison authority.
