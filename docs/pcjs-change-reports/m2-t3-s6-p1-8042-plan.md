# M2 T3 S6 P1 PCjs Change Report: 8042 Plan

## Summary

- Affected PCjs-derived subsystem: selected PC/AT 8042 keyboard controller.
- Changed product behavior: none; this part records a project-native plan.

## Basis

- PCjs establishes `0x60`/`0x64` ownership, selected C8042 status and command
  families, command-byte IRQ control, output-buffer behavior, and the
  output-port A20/reset relation.

## Boundary

- Future TypeScript code will preserve S5 ownership of `0x61` and output-port
  signals, use raw keyboard-byte ingress, and raise native PIC IRQ1 without a
  PCjs import, BIOS workaround, browser event dependency, or guest service.

## Future Path

- Executable parts will add a controller core, PC/AT adapter, native IRQ1
  composition, integration evidence, and a reset-only browser checkpoint.
