# M2 T4 S3 P1 PCjs Change Report: Fixed Storage Plan

PCjs establishes the selected AT controller's retained register, geometry, and
IRQ behavior. This planning part imports no PCjs source or runtime and adds no
controller, media, firmware, DOS, filesystem, or guest-service code.
