# M2 T4 S3 P2 PCjs Change Report: Raw Fixed Drive

PCjs provides the selected Type 5 geometry evidence. This part implements an
original TypeScript raw CHS media contract and imports no PCjs code or runtime.
It adds no controller, host path, firmware, DOS, filesystem, or guest service.
