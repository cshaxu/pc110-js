# M2 T5 P25 PCjs Change Report: VGA Option ROM Entry

PCjs is not changed or imported. Its selected VGA model accepts the five-bit
attribute index and retains its full indexed data bank, and its DeskPro M1
configuration maps the VGA ROM at `0xC0000`. This part implements equivalent
project-native register ownership and reference-trace mapping only.
