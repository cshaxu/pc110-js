# M2 T5 P29 PCjs Change Report

PCjs is not changed or imported at runtime. Its read-only PC tool documents
the IBM VGA `INT 10h` to `INT 6Dh` initialization relationship used to classify
the observed project-native ROM transfer. The trace-hook selection is wholly
project-native diagnostic infrastructure and changes no PCjs behavior.
