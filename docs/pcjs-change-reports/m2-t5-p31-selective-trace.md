# M2 T5 P31 PCjs Change Report

PCjs is unchanged and remains a read-only pinned comparison and ROM-source
reference. P31 changes only project-native TypeScript diagnostic observation;
it neither imports PCjs runtime code nor alters its behavior.
