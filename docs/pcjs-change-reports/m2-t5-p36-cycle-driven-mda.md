# M2 T5 P36 PCjs Change Report

PCjs is unchanged and not used at runtime. Its read-only video timing model
informed the status-bit semantics; its screen-off diagnostic workaround is
explicitly excluded.
