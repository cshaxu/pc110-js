# M2 T5 P66 PCjs Change Report: Bounded Checkpoint Replay

PCjs is unchanged. Its pinned ROM bytes remain an input and compatibility
reference. This project-native diagnostic runner reconstructs an independent
TypeScript machine for a short replay and does not import or execute PCjs code
at runtime.
