# M2 T5 S1 P12 PCjs Change Report: VGA DAC

PCjs models VGA DAC component transfers through read/write address ports and
auto-advancing RGB components. Original TypeScript now retains the same native
hardware-state boundary without importing PCjs runtime or renderer code.
