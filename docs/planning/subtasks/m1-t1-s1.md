# M1 T1 S1: Select PCjs PCx86 V2

## Objective

Confirm PCjs PCx86 v2 as the M1 implementation reference generation and state
the bounded consequence for M1 and M2.

## Non-Goals

- Do not select the exact PC/AT machine configuration.
- Do not add a runner, browser page, or emulator implementation.
- Do not copy or modify sibling PCjs source.

## Dependencies

- Read-only sibling checkout: `../pcjs`.
- Pinned upstream baseline:
  `c7f21b4fa2bdedac3d5c73094a6402fdc8b24c70`.
- Existing baseline record:
  `docs/baselines/pcjs-upstream-c7f21b4fa.md`.

## Expected Records

- Update the PCjs baseline record only if inspection contradicts its current v2
  selection or CPU ceiling.
- Add a compact verification record and tracking entry when the selection is
  confirmed.

## Verification

```powershell
git -c safe.directory=D:/home/repos.hobby/pcjs -C ../pcjs rev-parse c7f21b4fa2bdedac3d5c73094a6402fdc8b24c70^{commit}
rg -n "PCx86|80386|x86" ../pcjs/machines/pcx86
```

## Completion Evidence

The pinned commit exists in the sibling checkout, the v2 source generation is
present, and documentation records that PCjs supports through the 80386 for the
generic reference baseline.

## Stop Conditions

Stop and request owner direction if the pinned commit is unavailable, the v2
source generation is absent, its CPU support contradicts the baseline record,
or the reference checkout requires modification to inspect it.
