# M1 T1 S2: Verify Pinned PCjs Checkout

## Objective

Verify that the read-only sibling checkout exposes the pinned PCjs commit and
record how reference-mode work remains independent of its current branch.

## Non-Goals

- Do not change the sibling checkout, branch, index, worktree, or Git config.
- Do not select the M1 machine configuration.
- Do not materialize PCjs source inside the tracked project tree.

## Dependencies

- Read-only sibling checkout: `../pcjs`.
- Pinned commit: `c7f21b4fa2bdedac3d5c73094a6402fdc8b24c70`.

## Expected Records

- A verification record describes sibling path, current checkout state, and
  pinned-object availability.
- The M1 reference-run boundary records that a later runner must use the pinned
  Git object rather than assume the sibling worktree HEAD is the baseline.

## Verification

```powershell
git -c safe.directory=D:/home/repos.hobby/pcjs -C ../pcjs rev-parse --show-toplevel
git -c safe.directory=D:/home/repos.hobby/pcjs -C ../pcjs status --short
git -c safe.directory=D:/home/repos.hobby/pcjs -C ../pcjs rev-parse origin/master
git -c safe.directory=D:/home/repos.hobby/pcjs -C ../pcjs cat-file -e c7f21b4fa2bdedac3d5c73094a6402fdc8b24c70^{commit}
```

## Completion Evidence

The sibling path is available and clean, the pinned object is present, and the
project can require an exact object without changing the user's active branch.

## Stop Conditions

Stop and request owner direction if the sibling path is unavailable, the pinned
object is absent, sibling inspection requires a write, or no read-only method
can later materialize the pinned source for browser serving.
