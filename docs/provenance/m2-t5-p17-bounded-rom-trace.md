# M2 T5 P17 Bounded ROM Trace Provenance

The trace recorder is original diagnostic TypeScript. It retains only the
requested tail and optional CS-transfer events, so it does not change native
CPU, memory, firmware, media, or device behavior.
