# M2 T5 P18 ROM Watch Provenance

The address-watch counter is original diagnostic TypeScript. It observes native
instruction trace events without changing dispatch, CPU state, timing, memory,
or device behavior.
