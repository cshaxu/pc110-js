# M2 T5 P22 Browser Throughput Provenance

The browser runner changes only host scheduling: it executes larger batches of
already-native instructions and throttles canvas/status repainting. CPU cycle
charges, device clocks, ROM bytes, media, and machine behavior are unchanged.
