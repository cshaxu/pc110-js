# M2 T5 P25 VGA Option ROM Entry Provenance

The TypeScript indexed-port transactions and retained attribute bank are
project-native. PCjs is used only by the existing diagnostic trace to read the
pinned selected ROM files from the adjacent local reference repository; neither
ROM bytes nor PCjs code are included in the product runtime.
