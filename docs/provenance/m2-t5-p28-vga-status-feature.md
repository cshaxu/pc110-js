# M2 T5 P28 VGA Status And Feature Provenance

The Input Status 0 and Feature Control implementations are original
TypeScript. PCjs `video.js` is consulted as a behavioral reference for the IBM
VGA ROM's documented SWSENSE/DAC probe and Feature Control port ownership. No
PCjs source, firmware bytes, or runtime dependency is added.
