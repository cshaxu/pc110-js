# M2 T5 P38 Provenance

The selected project-native ROM trace composes `deskProSecondaryPit`. P38
applies that same project-native machine configuration to the browser checkpoint
instead of allowing its ports to fall through to floating I/O.
