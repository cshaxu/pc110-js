# M2 T5 P39 Provenance

The browser run used only project-owned TypeScript CPU, memory, device, and
browser modules plus user-local media accepted by the existing hash contracts.
PCjs was not a runtime dependency.
