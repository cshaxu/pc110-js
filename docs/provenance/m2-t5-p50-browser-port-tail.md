# M2 T5 P50 Provenance

- Authority: P45 keyboard-admission diagnostic need and the owner-approved
  Fast/short-checkpoint workflow.
- Contract: `NativeCoreCheckpoint` records only the latest sixteen native port
  events and configures its machine trace with instruction snapshots disabled.
- Boundary: the ring is observational browser status. It changes no port
  handling, device state, timing, interrupt, firmware, or guest behavior.
