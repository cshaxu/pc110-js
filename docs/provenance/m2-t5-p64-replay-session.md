# M2 T5 P64 Provenance

- Authority: the diagnostic checkpoint plan requires ordered external input,
  configuration, ROM/media hashes, project identity, and virtual position in
  addition to core state.
- Contract: this in-memory session restores core and queued keyboard bytes only
  when all recorded identity fields match exactly.
- Boundary: it has no browser UI, host path, persistence, save file, PCjs
  runtime, or guest-service behavior.
