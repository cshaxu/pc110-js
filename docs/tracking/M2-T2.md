# M2 T2: TypeScript CPU, Memory, And Bus

This log records concise, commit-aligned M2 core-emulation progress.

## S1: Implement 80386 CPU State And Reset

- P1: Added a project-native generic 80386 reset-state model with PCjs
  provenance; [verification](../verification/m2-t2-s1-cpu-reset-state.md)
  passed.

## S2: Implement Modes And Paging

- P1: Added mode selection, cached-segment translation, and a two-level paging
  walker; full 80386 descriptor, fault, and TLB behavior remains active.
- P2: Added bounded GDT/LDT descriptor decoding; segment loading and privilege
  behavior remain active.
- P3: Added segment permission, privilege, and expand-down predicates; fault
  state and segment-cache integration remain active.
- P4: Added PDE/PTE accessed and dirty updates with structured page-fault
  metadata; CPU exception delivery and TLB behavior remain active.
- P5: Added CR3-flushed translation caching and CR2 page-fault capture; CPU
  exception delivery remains active.
- P6: Added mode-aware cached segment loading through validated GDT descriptors;
  LDT, gates, tasks, and exception delivery remain later CPU work.
- P7: Added selector TI-bit routing between the GDT and an explicitly supplied
  LDT; descriptor-table loading, gates, tasks, and exception delivery remain.
- P8: Added validation and extraction of present LDT system descriptors from the
  GDT; LDTR instruction state, gates, tasks, and exception delivery remain.
- P9: Added the dedicated protected-mode stack-segment type and privilege
  predicate; opcode dispatch, gates, tasks, and exception delivery remain.
- P10: Added independent GDTR and IDTR state with 80386-width base and limit
  normalization; descriptor-table instructions and interrupt delivery remain.
- P11: Added a raw 32-bit CR0 state-write boundary; privileged instruction
  checks, mode-transition side effects, and TLB coordination remain later work.
- P12: Added normalized EFLAGS state writes for virtual-8086 mode selection;
  `POPF` and `IRET` privilege semantics remain later instruction work.
- P13: Added multi-byte normal and expand-down segment-range validation;
  instruction memory accesses remain part of later CPU execution work.
- Completion: The mode, segmentation, descriptor-table, and paging contract
  passed [verification](../verification/m2-t2-s2-modes-paging.md). Instruction
  execution, exception delivery, gates, and tasks move to S3.

## S3: Implement Opcode Families And Privilege Behavior

- P452: Closed the NXVM executable-coverage audit within explicit TODO
  boundaries; the rebuilt ROM trace reaches the documented S5 I/O boundary and
  the recorded M1 PCjs reference reaches `A:\>`.

- P451: Added NXVM-style PDE/PTE permission, fault-metadata, and Accessed/Dirty
  matrix evidence; clarified that NXVM only defines debug-register transfers,
  not a hardware-breakpoint engine.

- P450: Completed focused code/data/stack descriptor fault-matrix evidence for
  type, presence, RPL/DPL, error-code, and no-commit behavior.

- P449: Preserved page faults from LLDT/LTR GDT descriptor reads while retaining
  `#GP(selector)` for invalid descriptor-table lookup.

- P448: Preserved page faults from LAR/LSL/VERR/VERW descriptor reads while
  retaining ZF failure only for invalid selector lookup.

- P447: Routed protected GDT/LDT/IDT/TSS and TSS I/O-map accesses through
  supervisor linear paging, with byte-correct page-table writeback.

- P446: Added NXVM-aligned whole-operand protected segment-limit preflight for
  16-bit and 32-bit multi-byte reads and writes before any byte is accessed.

- P445: Corrected rebuilt LLDT/LTR system-descriptor fault classification to
  deliver `#NP(selector)` for a non-present valid descriptor and preserve
  `#GP(selector)` for invalid selector/type paths.

- P444: Added NXVM-aligned target-code validation before rebuilt control-transfer
  commits, preserving source-EIP `#GP` delivery for an invalid protected FAR JMP.

- P443: Added NXVM-aligned GDT/LDT descriptor Accessed-bit writeback after
  successful protected code, data, and stack loads, with focused rejection coverage.

- P442: Completed the `TODO(High)` alignment for NXVM's I/O-map, interrupt,
  WAIT, and post-80386 undefined-handler TODO entries without changing their
  independently tested project-native execution behavior.

- P441: Aligned NXVM-marked task/call-gate and outer-RETF TODO boundaries with
  explicit project-native `TODO(High)` markers; reclassified them as exclusions
  rather than unowned S3 blockers without changing supported execution.

- P440: Restored speculative RETF stack pops before protected selector fault
  delivery and added focused CPL3-to-ring0 TSS-frame evidence.

- P439: Converted protected software-INT gate-DPL rejection from a host error
  into rebuilt `#GP` delivery with focused TSS-frame evidence.

- P438: Removed an unreachable v86 IRET branch and corrected P437 evidence to
  cover only the verified protected `#GP(selector)` path.

- P437: Replaced invalid protected IRET host errors with rebuilt `#GP` delivery
  and a focused CPL3-to-ring0 TSS-frame regression.

- P436: Corrected five opcode-ledger top-table rows to cite the existing
  complete P329, P331, P332, and P424 handler evidence rather than stale
  differential-evidence placeholders.

- P435: Reconciled the instruction manifest and 80386 matrix with verified
  P417-P434 evidence, distinguishing closed NXVM handlers from active shared
  architecture and later S4/S5/S6 closure work.

- P434: Re-ran the rebuilt selected-ROM trace; it reaches `F000:F907` and
  stops only at the documented unavailable project-native I/O bus boundary.

- P433: Added NXVM-style cross-page multi-byte memory preflight, preventing
  partial rebuilt writes when a later page translation faults.

- P432: Corrected relative-control target width to follow NXVM operand-size
  semantics for taken 0F near Jcc and E8/E9/EB paths, with high-EIP regressions.

- P431: Closed the NXVM prefix/decode boundary, including all prefix classes,
  default-size selection, 15-byte enforcement, and project-native `#GP(0)`
  delivery at the instruction-start EIP.

- P430: Closed executable NXVM `0F A0-BF` extended-handler coverage with
  default-32 arithmetic, bit, scan, extension, and ModR/M evidence; retained
  NXVM's fixed-r32 word-extension behavior.

- P429: Closed executable NXVM `0F 80-9F` conditional-handler coverage with
  default-32 SETcc memory addressing and `66` near-Jcc displacement evidence,
  joined to all-selector predicate, flags, ModR/M, and prefix coverage.

- P428: Closed executable NXVM `0F 00-26` system handler coverage with direct
  default-32 table-addressing and selector-width evidence, joined to existing
  descriptor, CPL, CR/DR/TR, TSS, and fault coverage.

- P427: Closed executable NXVM `E0-FF` handler coverage with default-32 loop
  and control-transfer evidence, joined to existing I/O, flags, privilege,
  arithmetic-fault, and Group Four/Five coverage.

- P426: Closed executable NXVM `D0-DF` handler coverage, including Group Two,
  AAM/AAD fault handling, default-32 XLAT, and explicit D8-DF `#UD` routing.

- P425: Closed executable NXVM `C0-CF` handler coverage with default-32 C1,
  C4/C5, C7, C2, and CA regressions, joined to existing group, frame,
  interrupt/IRET, selector, and access-fault evidence.

- P424: Closed executable NXVM `B0-BF` immediate-register MOV coverage across
  all registers, default-16/default-32 code, `66`, immediate length, and word
  upper-half preservation.

- P423: Closed executable NXVM `A0-AF` handler coverage with default-32,
  independent `66/67`, full generic-string, REP zero-count, and protected DS
  `#GP` fault-EIP evidence. Shared architecture dependencies remain separately
  ledgered.

- P422: Closed executable NXVM `90-9F` handler coverage, including a conforming
  protected far-control regression. NXVM-marked call/task-gate and cross-ring
  far-control TODOs remain explicit architecture dependencies.

- P421: Corrected POPF to derive CPL from the CS hidden cache, preserving IOPL
  when cached DPL and visible selector RPL differ. The `90-9F` family remains
  open for separate complete far-control architecture work.

- P420: Closed the `80-8F` family using its complete rebuilt execution and
  fault evidence. Added exhaustive legal `8C/8E` segment-register encoding
  coverage; invalid variants remain routed through rebuilt `#UD`.

- P419: Closed the complete `60-6F` family. Added the NXVM-aligned ARPL v86
  `#UD` correction and tested default sizes, `66/67`, FS/GS, REP, all string-I/O
  widths, privilege fault delivery, and independent stack behavior.

- P418: Added protected I/O admission for rebuilt scalar and string I/O. It
  enforces CPL/IOPL and v86 rules plus the 32-bit TSS bitmap before side effects,
  routing denial as `#GP(0)`; `60-6F` remains pending a separate family closure.

- P417: Closed the `00-3F` family with default-16/default-32/66/67 ALU matrix,
  v86 adjustment and segment-stack coverage, and protected selector #NP/#GP
  delivery evidence. `0F` remains separately ledgered as an escape opcode.

- P416: Extended rebuilt 16/32-bit TSS stack selection to target rings one and
  two. CPL3-to-CPL1/CPL2 interrupt and IRET regressions passed; task switching
  remains active work.

- P415: Added 16-bit available/busy TSS privilege-stack selection alongside
  the existing 32-bit path. Focused LTR, outer-privilege IRET, and virtual-8086
  interrupt-frame regressions passed; task switching remains active work.

- P397: Added rebuilt hidden-cache access attributes and protected normal/
  expand-down access enforcement, separating instruction fetch from data reads.
  v86 access semantics, segment-system fault codes, and task/gate paths remain open.

- P396: Structured rebuilt segment-loader faults as #GP/#NP/#SS with selector
  error codes and connected them to executor fault delivery. Gate/task and
  outer-privilege selector behavior remains active work.

- P395: Extended rebuilt #PF/#GP/#SS conversion across decode-time fetches.
  Fetch faults do not invoke the dispatcher and produce an explicit fault trace
  event; descriptor codes, escalation, and outer-privilege delivery remain open.

- P394: Converted typed rebuilt post-decode page and segment access failures
  into #PF/#GP/#SS delivery with fault EIP preservation. Protected page-fault
  frame coverage passes; fetch faults, descriptor codes, and escalation remain
  active event-system dependencies.

- P393: Completed NXVM `0F` undefined intervals `40-7F` and `C0-FF` through
  rebuilt vector-six delivery. The exhaustive undefined-extension regression
  keeps unimplemented non-NXVM families visible rather than relabeling them.

- P392: Routed rebuilt instruction and data access through the project-native
  80386 page-table walker, including accessed/dirty updates and CR2 recording.
  Vector-14 delivery, TLB behavior, and full segment permission checks remain
  active architecture dependencies.

- P391: Routed the `06/07/0E/16/17/1E/1F` protected-mode POP segment forms
  through rebuilt data/stack loaders. GDT-backed data and `66`/SS D/B stack
  cases passed focused tests; descriptor-fault delivery and POP-SS event
  inhibition remain active dependencies.

- P366: Corrected the handoff ordering. S6 is the final M2 T2 integration gate
  after S3 CPU coverage, S4 memory, and S5 I/O/reset/stepping close; it does not
  bypass the remaining T2 subtasks.
- P365: Defined the former CPU handoff rule. P2 supersedes its device-proxy
  method with test-only one-instruction PCjs CPU differential validation;
  PCjs remains prohibited in product runtime paths.
- P364: Corrected execution governance after the CPU reconstruction review.
  Breadth-first ROI delivery remains the default for integration and devices;
  M2 T2 CPU work is architecture-closure work and proceeds by the NXVM ledger's
  numeric families and recorded dependencies. NXVM is the decisive CPU behavior
  authority, while PCjs remains the PC/AT and whole-machine comparison source.
- P320: Froze the verified incremental CPU as `cpu-legacy-reference` at
  `26bd074` and replaced incremental execution-context migration with a
  project-native, NXVM-ordered opcode-family reconstruction plan and ledger.
  Existing CPU tests, ROM trace evidence, and behavior remain differential
  reference only; no runtime behavior changed.
- P321: Added an isolated rebuilt CPU state, prefix, event, and trace scaffold
  with independent focused tests. It does not execute instructions or replace
  the legacy machine runtime; the prefix family remains in progress.
- P322: Added a rebuilt instruction decode boundary that derives both default
  sizes from CS D/B, preserves the instruction-start EIP for overlength faults,
  and records final segment or repeat prefixes. It still does not execute or
  complete an opcode family.
- P323: Added independent 16-bit and 32-bit ModR/M effective-address decoding,
  including SIB, displacement, and default DS/SS selection. It remains shared
  infrastructure and does not execute or complete an arithmetic family.
- P324: Added rebuilt mutable state accessors and segmented byte/word/dword
  operand access. Code EIP width, memory address width, and segment cache
  translation remain distinct; paging and protection-fault delivery remain
  later rebuilt-core work.
- P325: Added shared rebuilt arithmetic semantics for the `00-3F` ADD/ADC,
  SUB/SBB/CMP, and logical forms across byte, word, and dword widths. Opcode
  dispatch and the complete `00-3F` execution family remain active work.
- P326: Added a rebuilt single-step execution boundary that fetches from the
  cached reset CS:EIP, decodes instruction-local prefixes, preserves the
  instruction-start snapshot, and exposes trace hooks. Opcode dispatch remains
  intentionally external until the complete `00-3F` family is ready.
- P327: Added rebuilt execution for the `00-3D` ALU patterns, four adjust
  opcodes, and real-mode segment-stack forms. Protected-mode selector loading
  and fault delivery remain explicitly incomplete, so `00-3F` stays partial.
- P328: Recorded the owner-authorized interval-delivery correction. P327 stays
  a verified partial slice; subsequent rebuilt CPU parts begin from an NXVM
  handler checklist and deliver executable large numeric intervals or complete
  families, with only genuinely dependent work deferred in the ledger.
- P329: Implemented the complete register-only `40-5F` interval from NXVM's
  INC, DEC, PUSH, and POP handler coverage. Operand data width follows CS plus
  `66`; SS D/B independently selects stack addressing. Tests cover every
  encoding, carry preservation, overflow, stack wrap, and ESP edge behavior.
- P330: Implemented the executable `60/61/68-6B` slice: PUSHA/POPA, PUSH
  immediates, and signed three-operand IMUL. The ledger retains `62-67` and
  `6C-6F` as explicit exception, selector, and I/O dependencies; `60-6F` is
  not complete.
- P331: Implemented the complete `70-7F` short-Jcc interval from NXVM's
  condition-handler coverage. Tests cover all predicates, taken and
  fallthrough paths, signed rel8 movement, CS-width EIP wrapping, and `66`.
- P332: Implemented NXVM Group One `80/81/83` immediate arithmetic: all eight
  extensions, byte/word/dword widths, sign-extended `83`, ModR/M memory, and
  address/segment prefixes.
- P333: Implemented the complete `84/85` TEST ModR/M family with defined
  logical flags and no operand writeback. The remaining `86-8F` move, segment,
  LEA, and POP forms remain active work.
- P334: Implemented the complete `86/87` XCHG ModR/M family. Tests cover byte
  and dword register/memory exchange, prefixes, and unchanged EFLAGS.
- P335: Implemented the complete `88-8B` general-register MOV ModR/M family,
  including both transfer directions, memory addressing, and operand/address
  size plus segment prefixes.
- P336: Implemented `8D` LEA with 16/32-bit effective-address selection, SIB,
  and register-only form rejection; segment-register MOV and POP r/m remain.
- P337: Implemented `90-97` NOP and accumulator XCHG, including every general
  register, `66` dword selection, EIP length, and unchanged EFLAGS.
- P338: Implemented `98/99` CBW/CWDE and CWD/CDQ with default-16 and `66`
  dword selection, signed results, and unchanged EFLAGS.
- P339: Implemented `9E/9F` SAHF/LAHF selected flag transfer, including LAHF
  fixed bit 1 and preservation of all non-transferred EFLAGS bits.
- P340: Implemented all `B0-BF` immediate register MOV encodings, including
  every byte and word register plus `66` dword immediates and EIP length.
- P341: Implemented all `A0-A3` accumulator moffs MOV forms, including byte
  and dword load/store, DS default, segment override, and 16/32-bit offsets.
- P342: Implemented `A8/A9` accumulator immediate TEST with byte/dword
  selection, no writeback, defined logical flags, and `66` coverage.
- P343: Implemented C6/C7 `/0` immediate ModR/M MOV across byte/word/dword
  register/memory forms and prefixes; `/1-7` await rebuilt `#UD` delivery.
- P344: Implemented D4/D5 AAM/AAD normal execution and flags; AAM base zero
  remains pending rebuilt `#DE` delivery.
- P345: Implemented D7 XLAT with BX/EBX selection, AL indexing, 16-bit wrap,
  address-size and segment overrides, and unchanged EFLAGS.
- P346: Implemented E0-E3 LOOP/LOOPE/LOOPNE/JCXZ with conditions, CX/ECX
  selection, rel8 control flow, `67`, and correct counter mutation boundaries.
- P347: Implemented E8/E9/EB near CALL and relative JMP forms, including
  short displacement, `66`, return-frame push, and independent SS D/B width.
- P348: Implemented C2/C3 near returns and C8/C9 ENTER/LEAVE as a rebuilt
  stack-frame control slice. Far returns and interrupt delivery remain blocked
  on selector validation and protection-fault reconstruction.
- P349: Implemented F6/F7 Group Three TEST, NOT, NEG, MUL, IMUL, DIV, and
  IDIV across byte/word/dword widths. Divide errors preserve fault EIP pending
  vector delivery; `/1` remains an explicit rebuilt `#UD` dependency.
- P350: Implemented FE/FF local Group Four/Five forms: INC, DEC, near CALL,
  near JMP, and PUSH. Far forms remain gated on rebuilt selector loading and
  protection-fault delivery.
- P351: Implemented complete C0/C1/D0-D3 Group Two rotate and shift forms,
  including immediate, one, and CL counts across byte/word/dword widths.
- P352: Implemented local CMC/CLC/STC/CLD/STD flag-control forms. HLT, CLI,
  and STI remain gated on privilege checks and event-delivery reconstruction.
- P353: Added generic 0F escape decode and implemented the complete 0F 80-8F
  near-Jcc family with condition, width, and EIP coverage.
- P354: Implemented complete 0F 90-9F SETcc with register and segmented-memory
  byte destinations while preserving EFLAGS.
- P355: Added the rebuilt opcode dispatcher and a mixed instruction sequence
  test. Machine-runtime selection remains an explicit later integration gate.
- P356: Added an independent rebuilt runner and verified reset-ROM alias
  execution without the legacy CPU runtime.
- P357: Completed the direct real-mode `EA` far-JMP form in the existing
  E8-EB control-transfer interval, including operand-selected offset width and
  an explicit protected-mode selector-validation boundary.
- P358: Added a local-only rebuilt selected-ROM trace command. It reaches the
  first real I/O boundary at `F000:F907` without synthetic port behavior.
- P359: Implemented the rebuilt E4-E7/EC-EF port-I/O interval with a
  width-aware project bus; protected-mode I/O permission checks remain pending.
- P360: Implemented rebuilt GDT-backed data-segment loading for 8C/8E/C4/C5.
- P361: Implemented rebuilt direct far CALL/JMP and same-privilege RETF forms.

- P1: Established the execution record, source boundary, delivery order, and
  verification requirements; implementation is active.
- P2: Added a project-native CS:EIP single-byte opcode-fetch contract with a
  focused reset-vector test; decode and execution remain active.
- P3: Added NOP and HLT execution, EIP commit, halted state, and unsupported
  opcode preservation; general decoding and exception delivery remain active.
- P4: Added real-mode `JMP FAR ptr16:16` for reset-vector control transfers;
  protected-mode far jumps remain active privilege work.
- P5: Added real-mode `MOV r16, imm16`, `JMP rel8`, and `JMP rel16` execution
  with 16-bit IP wrapping; operand-size prefixes remain active decode work.
- P6: Added `MOV r8, imm8` and `OUT imm8, AL` with a port-writer boundary for
  the observed reset-ROM path; actual port dispatch remains T2 S5 work.
- P7: Added SAHF and real-mode CLI flag transitions from the observed ROM path;
  protected-mode CLI remains active privilege work.
- P8: Added the observed `0F 01 F0` register-form LMSW behavior with PE and
  fixed-bit preservation; general ModR/M and memory forms remain active work.
- P9: Added real-mode register-form `MOV Sreg, r16` for the observed DS load;
  protected-mode and memory forms remain active work.
- P10: Added `IN AL, imm8`, `TEST AL, imm8`, and `JNZ rel8` through the narrow
  port-I/O boundary; concrete port dispatch remains T2 S5 work.
- P11: Added 16-bit `LOOP rel8` for the observed ROM polling path; address-size
  override and 32-bit count forms remain active decode work.
- P12: Added reusable ModR/M decoding plus register-form `MOV r16, r/m16` and
  `XOR r16, r/m16`; memory addressing remains active work.
- P13: Added register-form `CMP r/m8, imm8` (`80 /7 ib`) and full 8-bit
  subtraction flags for the observed POST branch; memory forms remain active.
- P14: Added `JBE rel8` and register-form `XOR r8, r/m8` for the next observed
  ROM control flow; memory forms remain active work.
- P15: Added `SHL r8, 1` (`D0 /4`) and `JCXZ rel8` for the observed ROM path;
  count and memory forms remain active decode work.
- P16: Added the observed CS-overridden `JMP FAR m16:16` reset-ROM table path;
  generalized prefix and memory operand decoding remain active work.
- Status: Paused after P16 by the 2026-07-29 authorized dependency correction.
  The trace reaches a far jump into low RAM, so bounded S4 memory enablement
  had to pass the existing trace before S3 resumed.
- Status: Resumed after S4 P4 verified the mapped-ROM trace. Remaining opcode,
  exception, interrupt, and privilege work retains the original S3 scope.
- P17: Restricted NXVM to M2-only structure, CPU data-model, coverage-planning,
  and trace-design reference use. PCjs remains the behavior authority; NXVM
  POST, BIOS, device, platform, and guest-service logic is excluded.
- P18: Added reusable 16-bit ModR/M effective-address decoding and the
  memory-source `MOV r16, r/m16` form with direct, indexed, displacement, and
  BP-default-SS coverage.
- P19: Replaced the reset-ROM far-jump table special case with real-mode
  `FF /5 m16:16` decoding through default or CS-overridden memory segments.
- P20: Added `CMP r/m16, imm16` (`81 /7 iw`) and complete 16-bit comparison
  flags for the observed reset-ROM extension signature check.
- P21: Added CS-overridden `MOV r16, r/m16` through the shared 16-bit ModR/M
  path, covering the observed reset-ROM table load.
- P22: Extended real-mode `MOV Sreg, r/m16` to memory sources through the
  shared 16-bit ModR/M decoder.
- P23: Added `MOV r/m16, r16` through the writable-memory boundary while ROM
  immutability remains enforced by physical memory.
- P24: Added register and memory forms of 8-bit `MOV` through the shared
  ModR/M and writable-memory boundaries.
- P25: Added immediate AL `OR`, `AND`, and `CMP` with focused flag coverage.
- P26: Added zero-flag `JZ rel8` control flow for ROM polling branches.
- P27: Added real-mode near `CALL rel16` and `RET` through SS:SP.
- P28: Added real-mode 16-bit general-register `PUSH` and `POP` through SS:SP.
- P29: Added direction-flag control and real-mode `REP STOSW` through ES:DI.
- P30: Added real-mode `REP MOVSW` through DS:SI and ES:DI.
- P31: Added real-mode `INT imm8` delivery through the IVT and SS:SP.
- P32: Added real-mode `IRET` restoration of IP, CS, FLAGS, and SS:SP.
- P33: Added DX-addressed 8-bit port reads and writes through `PortIo`.
- P34: Added real-mode `PUSHA` and `POPA` through SS:SP.
- P35: Added carry-preserving 16-bit register `INC` and `DEC`.
- P36: Added `CLC`, `STC`, and carry-flag `JC rel8` control flow.
- P37: Added 16-bit `LEA r16, m` through the shared ModR/M address decoder.
- P38: Added DS-based accumulator `MOV moffs` loads and stores (`A0` through
  `A3`) for observed ROM data access.
- P39: Added immediate byte and word writes through register or memory ModR/M
  operands (`C6 /0` and `C7 /0`) for observed POST initialization.
- P40: Added real-mode segment-selector stores to register and memory ModR/M
  operands (`8C`) for observed descriptor and workspace setup.
- P41: Added the observed `ES:`-overridden immediate memory initialization
  forms (`26 C6 /0` and `26 C7 /0`).
- P42: Added `LODSB` through DS:SI with direction-flag controlled indexing for
  the observed reset-ROM port-table loop.
- P43: Added `OR r8, r/m8` (`0A`) through the shared ModR/M decoder and
  8-bit logic-flag contract for the observed polling loop.
- P44: Added `CMP r/m16, imm8` (`83 /7 ib`) with signed-byte immediate
  expansion for the observed reset-ROM shutdown dispatch.
- P45: Added an instruction-level PCjs reset-path regression from `FFFF:FFF0`
  through the `E000:0003` option-ROM dispatch using controlled port responses.
- P46: Removed duplicated incomplete-scope text from the execution provenance
  record and aligned it with active S5 port-routing work.
- P47: Added byte and word `XCHG r/m, r` forms with register and memory
  coverage for observed DeskPro BIOS routines.
- P48: Added real-mode segment-register PUSH and POP through SS:SP for
  observed BIOS save and restore paths.
- P49: Added `ADD AX, imm16` and complete 16-bit addition flags for observed
  DeskPro BIOS memory-probe arithmetic.
- P50: Added byte ADD and SUB ModR/M forms and immediate 80-group add,
  subtract, and compare forms for observed BIOS table and descriptor updates.
- P51: Added 16-bit SHL by one and immediate count through register and memory
  operands for observed DeskPro BIOS arithmetic paths.
- P52: Added the observed `ES:`-overridden memory-source `MOV r16, r/m16`
  form for the DeskPro BIOS memory-probe loop.
- P53: Added byte `CMP r/m8, r8` through register and memory operands for the
  observed DeskPro BIOS descriptor probe.
- P54: Added unsigned word DIV through DX:AX with explicit divide-by-zero and
  quotient-overflow fault boundaries for observed BIOS display arithmetic.
- P55: Added real-mode PUSHF, POPF, and STI for BIOS flag-save and interrupt
  enable paths; protected-mode privilege behavior remains active work.
- P56: Added immediate byte ADC and unsigned word MUL for observed descriptor
  initialization; multiply updates only its defined CF/OF flags.
- P57: Added logical byte and word right shifts by one or immediate count for
  observed DeskPro descriptor initialization.
- P58: Added a PCjs descriptor-initialization regression through segment setup,
  REP STOSW, MUL, ADD/ADC, and the first descriptor write; corrected real-mode
  sequential EIP wrapping exposed by its reset-alias crossing.
- P59: Added real-mode address-size LGDT and LIDT memory forms for the BIOS
  protected-mode transition descriptor tables.
- P60: Added register-direct CR0 reads and writes for observed DeskPro
  protected-mode transition code; privilege and TLB side effects remain active.
- P61: Added `OR AX, imm16` for the observed protected-mode PE-bit setup
  sequence through the shared logic-flag contract.
- P62: Connected checked GDT code-descriptor loading to 16-bit protected-mode
  far jumps, including the observed memory-pointer form.
- P63: Connected protected-mode `MOV Sreg, r/m16` to checked data and stack
  descriptor loading, covering the DeskPro selector `0x08` data descriptor.
- P64: Added the observed ES-overridden byte-memory MOV through the shared
  ModR/M write path and cached protected ES base.
- P65: Added the observed operand-size-overridden `AND EAX, imm32` form and
  the shared 32-bit logic-flag contract.
- P66: Added an integrated regression for the observed CR0-based return from
  protected mode through the real-mode far jump.
- P67: Added the observed 16-bit `JMP r/m16` form used after the return-path
  `LIDT` sequence.
- P68: Enabled protected-mode CPL 0 CLI/STI while retaining explicit nonzero-CPL
  fault boundaries pending exception delivery.
- P69: Added byte and word register-destination XOR memory forms through shared
  ModR/M, segment, and logic-flag paths.
- P70: Added byte AND immediate group form `80 /4` for register and memory
  operands on observed ROM masking paths.
- P71: Added byte `TEST r/m8, imm8` (`F6 /0`) for observed ROM bit tests,
  preserving register and memory operands while updating logic flags.
- P72: Added word `TEST r/m16, imm16` (`F7 /0`) for observed ROM status-word
  tests, preserving register and memory operands while updating logic flags.
- P73: Added byte `TEST r/m8, r8` (`84 /r`) for observed ROM branch predicates,
  preserving both register and memory operands while updating logic flags.
- P74: Added word `TEST r/m16, r16` (`85 /r`) for observed ROM branch predicates,
  preserving both register and memory operands while updating logic flags.
- P75: Completed the short conditional-jump family (`70` through `7F`) through
  shared EFLAGS predicates, including taken and not-taken coverage for every form.
- P76: Added `OR r16, r/m16` (`0B /r`) for observed ROM state tests with
  register and memory sources through the shared logic-flag contract.
- P77: Added `OR r/m16, imm16` (`81 /1`) for observed ROM state-word updates,
  with register and direct-memory destinations through existing write boundaries.
- P78: Added `OR r/m16, r16` (`09 /r`) for observed ROM work-area updates,
  with register and direct-memory destinations through existing write boundaries.
- P79: Added `OR r/m8, r8` (`08 /r`) for observed ROM state-byte updates,
  with register and direct-memory destinations through existing write boundaries.
- P80: Routed real-mode software interrupts and divide errors through a shared
  IVT delivery path; divide faults now preserve the faulting IP instead of
  escaping as host-only errors.
- P81: Added a device-facing real-mode external-interrupt service boundary that
  honors IF, shares IVT delivery, preserves current EIP, and wakes HLT.
- P82: Added real-mode `FF /3` far calls with a CS:IP stack frame and corrected
  FF-group dispatch so unsupported forms no longer execute as far jumps.
- P83: Added real-mode `RETF` and `RETF imm16`, restoring CS:IP and applying
  the optional caller-stack cleanup for ROM far-call return paths.
- P84: Added real-mode `FF /2` near calls for register and CS-overridden memory
  targets, saving a prefix-inclusive return IP through SS:SP.
- P85: Added `FF /6` register and default-segment memory pushes through SS:SP,
  including the observed BP-default-SS source path.
- P86: Added the observed ES-overridden `FF /6` memory push while retaining
  separate ES source and SS stack boundaries.
- P87: Added word `ADD` ModR/M forms (`01` and `03`) for register and memory
  destinations through the existing 16-bit addition-flag and write boundaries.
- P88: Added `AND r/m16, imm16` (`81 /4`) for register and memory status-word
  masks without changing the existing OR and CMP immediate group forms.
- P89: Added `AND r/m16, r16` (`21 /r`) for register and memory work-area
  masks through existing write and logic-flag boundaries.
- P90: Added default-segment `POP r/m16` (`8F /0`) for observed BIOS stack
  restoration into decoded memory destinations.
- P91: Added ES-overridden `POP r/m16`, preserving separate SS stack and ES
  destination-memory boundaries.
- P92: Added `SUB r16, r/m16` (`2B /r`) for register-destination ROM counter
  and address arithmetic with register and memory source coverage.
- P93: Added signed-byte immediate `SUB r/m16, imm8` (`83 /5`) for register and
  memory stack or pointer adjustments.
- P94: Added signed-byte immediate `ADD r/m16, imm8` (`83 /0`) for register and
  memory stack or pointer adjustments.
- P95: Added immediate accumulator subtraction for `AL` and `AX` through the
  existing 8-bit and 16-bit subtraction-flag contracts.
- P96: Added `AND AX, imm16` for ROM state masks with focused zero and sign
  logic-flag coverage.
- P97: Added `ADD AL, imm8` with focused signed-overflow and carry-boundary
  flag coverage for observed ROM byte arithmetic.
- P98: Added `CMP AX, imm16` for ROM decision paths without modifying AX,
  with no-borrow and borrow flag coverage.
- P99: Added `ADC AX, imm16` and explicit 16-bit carry-in flag coverage for
  the ROM arithmetic path.
- P100: Added `XOR AL, imm8` with focused result and logic-flag coverage for
  ROM byte-state manipulation.
- P101: Added `AND r8, r/m8` (`22 /r`) for register and default-segment memory
  ROM state masks.
- P102: Added the observed ES-overridden byte `AND r8, r/m8` form while keeping
  unsupported ES prefix forms explicit.
- P103: Added byte and word register-destination ModR/M comparisons for ROM
  decision paths without changing either operand.
- P104: Shared the `CMP r, r/m` read path and added observed CS-overridden byte
  and word comparison forms.
- P105: Added `AND r/m8, r8` (`20 /r`) for byte register and memory state
  updates through shared write and logic-flag boundaries.
- P106: Added `XOR r/m8, r8` (`30 /r`) for byte register and memory state
  updates through shared write and logic-flag boundaries.
- P107: Added the observed ES-overridden byte `XOR r/m8, r8` form while keeping
  unsupported ES prefix forms explicit.
- P108: Added byte and word F6/F7 `NOT` and `NEG` forms for register and memory
  operands, including flag-preserving NOT and subtraction-flag NEG behavior.
- P109: Added borrow-aware byte subtraction flags and `SBB r/m8, imm8` (`80 /3`)
  for register and memory ROM arithmetic.
- P110: Added borrow-aware word subtraction flags and `SBB r16, r/m16` (`1B /r`)
  for register and memory ROM arithmetic.
- P111: Added `SBB AL, imm8` and `SBB AX, imm16` for observed ROM accumulator
  arithmetic with carry-in coverage.
- P112: Added byte and word register-destination ModR/M ADC forms for observed
  ROM multiword arithmetic with carry-in coverage.
- P113: Added accumulator and signed-byte Group 1 immediate ADC forms for
  observed ROM multiword arithmetic.
- P114: Added 8-bit F6-group unsigned MUL and DIV through AL/AX for observed
  ROM arithmetic and conversion paths.
- P115: Added accumulator-word TEST for observed ROM flag checks while
  preserving AX.
- P116: Added CBW for observed ROM byte-to-word signed conversion paths.
- P117: Added direct-register SAR by one for observed ROM signed-byte paths.
- P118: Added REP MOVSB and REP STOSB for observed ROM byte-string paths.
- P119: Added unprefixed MOVS and STOS byte/word forms for observed ROM paths.
- P120: Added observed CS-source MOVSW, REP MOVSW, and LODSW forms.
- P121: Bounded real-ROM trace classified sparse physical-memory mapping as a
  planned S4 prerequisite before actual ROM instruction execution can begin.
- P122: Added CS byte moffs load/store forms reached by the bounded ROM trace.
- P123: Added direct-register 8-bit INC reached by the bounded ROM trace.
- P124: Bounded ROM trace reached the unmapped VGA text-memory write boundary;
  no additional CPU opcode gap was observed.
- P125: Added the observed CS-overridden byte ModR/M load form.
- P126: Added observed 8-bit FE-group DEC register and memory forms.
- P127: Added observed single-bit byte RCL and RCR register forms.
- P128: Added observed byte and word ROL register forms through CL.
- P129: Added observed CMPS and SCAS forms with REPE and REPNE termination.
- P130: Added observed AX-to-register short XCHG forms.
- P131: Added `DAA` and `DAS` (`27` and `2F`) with PCjs-derived decimal-adjust
  flag behavior: result `PF/ZF/SF`, explicit `CF/AF`, and cleared `OF`.
- P132: Added observed `AAD imm8` (`D5 ib`) with PCjs-derived byte arithmetic
  flags and the required `AH` clear.
- P133: Corrected `C0/C1` zero-count shifts to preserve operands and EFLAGS,
  matching the PCjs normalized-count behavior.
- P134: Added `LAHF` (`9F`) to transfer the current low EFLAGS byte into AH
  without changing architectural flags.
- P135: Added `CWD` (`99`) to sign-extend AX into DX while preserving EFLAGS.
- P136: Added no-FPU `WAIT/FWAIT` (`9B`) execution for the observed ROM path.
- P137: Added `AAM imm8` (`D4 ib`) with real-mode zero-divisor exception delivery.
- P138: Added `AAA` and `AAS` (`37` and `3F`) with PCjs 80286+ adjustment semantics.
- P139: Added `XLAT` (`D7`) through a 16-bit wrapped DS:BX table lookup.
- P140: Added immediate-word and sign-extended immediate-byte PUSH forms (`68`, `6A`).
- P141: Added `LEAVE` (`C9`) through the existing SS:SP stack boundary.
- P142: Added real-mode immediate far CALL (`9A`) with RETF-compatible stack order.
- P143: Added `LOOPNE` and `LOOPE` (`E0`, `E1`) through the existing CX loop path.
- P144: Added real-mode `INTO` (`CE`) through the existing vector-4 interrupt path.
- P145: Added 16-bit `MOVZX/MOVSX` source-width conversion forms (`0F B6/B7/BE/BF`).
- P146: Added 16-bit `BSF/BSR` (`0F BC/BD`) with PCjs zero-source behavior.
- P147: Added carry-complement `CMC` (`F5`) through the existing flag state API.
- P148: Added real-mode single-byte breakpoint interrupt (`CC`).
- P149: Added complete 16-bit nested `ENTER` stack-frame construction (`C8`).
- P150: Added real-mode signed memory `BOUND` (`62`) with vector-5 range faults.
- P151: Added signed 16-bit two-operand `IMUL` (`0F AF`) with CF/OF overflow behavior.
- P152: Added 16-bit `BT`, `BTS`, `BTR`, and `BTC` (`0F A3/AB/B3/BB`) for
  register and memory operands, including full signed register bit indexes for
  memory bit strings.
- P153: Added immediate Group 8 bit operations (`0F BA /4-/7 ib`) with
  PCjs-compatible low-four-bit index masking for 16-bit register and memory operands.
- P154: Added all `SETcc r/m8` forms (`0F 90-9F`) through the shared condition
  mapping, preserving EFLAGS for register and memory destinations.
- P155: Added all 16-bit near conditional jumps (`0F 80-8F cw`) through the
  shared condition mapping, with PCjs-compatible wrapped target calculation.
- P156: Added `LSS`, `LFS`, and `LGS m16:16` (`0F B2/B4/B5`) through the
  mode-aware segment-loader boundary.
- P157: Added 16-bit `PUSH/POP FS/GS` (`0F A0/A1/A8/A9`) through the existing
  SS:SP and mode-aware segment-loader boundaries.
- P158: Added `SMSW r/m16` (`0F 01 /4`) through existing ModR/M register and
  memory-write boundaries.
- P159: Added real-mode `UD2` (`0F 0B`) invalid-opcode fault delivery through
  IVT vector 6, preserving the faulting instruction pointer.
- P160: Added 16-bit `SHLD/SHRD` immediate and CL forms (`0F A4/A5/AC/AD`) for
  register and memory operands, including PCjs cross-word count behavior.
- P161: Added `ADC r/m16,r16` (`11 /r`) for register and memory destinations
  through the existing carry-aware addition boundary.

## S4: Implement Physical Memory And A20

- P8: Closed the final S4 contract with selected M1 map, A20, ROM-alias,
  rebuilt-memory-bus, and ROM-trace evidence; S5 owns the next I/O boundary.

- P7: Added explicit discontiguous RAM regions and verified the selected M1
  DeskPro low-RAM, RAM-window, and extended-RAM map without device behavior.

- P1: Recorded the owner-authorized dependency correction. S4 now provides the
  minimal memory prerequisite for the existing S3 ROM trace without changing
  the M2 completion gate.
- P2: Completed the behavior-preserving module-layout refactor. NXVM is
  recorded as a secondary CPU architecture and trace-design reference; PCjs
  remains the M2 behavior authority.
- P3: Added synthetic-fixture firmware image loading plus bounded low RAM,
  ROM-window aliases, immutable ROM writes, A20 gating, and page-table-width
  physical-memory access. The existing CPU trace verification follows in P4.
- P4: Re-ran S3 P16's reset-ROM far-jump table trace through the mapped-ROM
  physical-memory path. The trace preserved its `F000:1234` result, so S3 CPU
  work is eligible to resume.
- P5: Completed the bounded preemption and returned active work to S3. The
  remaining broader S4 scope remains scheduled after the incomplete S3 work.
- P6: Corrected real-mode instruction translation to use the cached reset CS
  base and verified the high ROM alias at `0xfffffff0`. The P16 far-jump trace
  still reaches `F000:1234` through the same mapped firmware image.
- P162: Added `SBB r/m16,r16` (`19 /r`) for register and memory destinations
  through the existing borrow-aware subtraction boundary.
- P163: Completed the byte `ADC/SBB` ModR/M directions (`10`, `12`, `18`, and
  `1A`) through the carry-aware byte ALU boundary and corrected carry-inclusive
  8/16-bit addition flags.
- P164: Added `SUB r/m8,r8` and `SUB r/m16,r16` (`28` and `29`) through shared
  byte and word ModR/M arithmetic boundaries.
- P165: Added `AND r16,r/m16` (`23 /r`) for register and memory sources through
  the existing 16-bit logic-flag boundary.
- P166: Added `XOR r/m16,r16` (`31 /r`) and `XOR AX,imm16` (`35 iw`) through
  the existing writable-memory and 16-bit logic-flag boundaries.
- P167: Added `CMP r/m16,r16` (`39 /r`) for register and memory operands
  without modifying the compared values.
- P168: Added operand-size-overridden 32-bit accumulator ALU forms for `ADD`,
  `OR`, `ADC`, `SBB`, `AND`, `SUB`, `XOR`, and `CMP`, with 32-bit arithmetic
  flag contracts.
- P169: Added operand-size-overridden `MOV r32,imm32` (`66 B8+rd id`) for all
  general registers.
- P170: Added operand-size-overridden `MOV r/m32,r32` and `MOV r32,r/m32`
  (`66 89/8B`) through default 16-bit ModR/M address calculation.
- P171: Added a standalone 32-bit ModR/M/SIB address decoder with explicit
  base, index, displacement, and default-segment behavior.
- P172: Connected `67` address-size decoding to protected-mode 32-bit MOV
  memory forms, accepting either `66 67` or `67 66` prefix order.
- P173: Added IDT interrupt/trap gate decoding with descriptor-table bounds,
  type, selector, offset, privilege, and present-state extraction.
- P174: Added the hidden `D/B` cache attribute to project segment state and
  propagated it through protected-mode descriptor loading.
- P175: Added same-privilege 32-bit protected-mode external interrupt delivery
  through real IDT/GDT descriptors and an ESP stack frame.
- P176: Added same-privilege protected-mode `INT`, `INT3`, and `INTO` delivery
  through IDT gates with software gate-DPL validation.
- P177: Added same-privilege 32-bit protected-mode `IRET` frame restoration.
- P178: Added same-privilege 32-bit protected-mode far call and far return.
- P179: Routed no-error-code protected-mode CPU faults through 32-bit IDT gates.
- P180: Routed the existing protected-mode 16-bit `BOUND` fault through vector five.
- P181: Added same-privilege 16-bit protected-mode interrupt frames and `IRET`.
- P182: Implemented protected-mode trap-gate IF/TF entry semantics.
- P183: Added the 32-bit operand-size-overridden `BOUND` limit check.
- P184: Added 32-bit address-size-overridden `BOUND` through the shared ModR/M decoder.
- P185: Added operand-size-overridden `CDQ` sign extension.
- P186: Added operand-size-overridden `CWDE` sign extension.
- P187: Added 32-bit protected-mode near call and return frames.
- P188: Added 32-bit protected-mode near-return caller cleanup.
- P189: Added 32-bit protected-mode `PUSHAD` and `POPAD` register frames.
- P190: Added 32-bit protected-mode register `PUSH` and `POP` forms.
- P191: Added operand-size-overridden 32-bit register `INC` and `DEC`.
- P192: Added 32-bit protected-mode near conditional-jump coverage.
- P193: Added operand-size-overridden 32-bit near jumps.
- P194: Added 32-bit ModR/M ADD and SUB execution foundations.
- P195: Added 32-bit ModR/M ADC and SBB with carry propagation through both address sizes.
- P196: Added 32-bit ModR/M OR, AND, XOR, and CMP through both address sizes.
- P197: Added 32-bit ModR/M TEST without operand writeback through both address sizes.
- P198: Added 32-bit immediate TEST through both address sizes without operand writeback.
- P199: Added 32-bit immediate ModR/M MOV through both address sizes.
- P200: Added all 32-bit Group 1 immediate ALU forms through both address sizes.
- P201: Added 32-bit immediate PUSH forms through the protected stack path.
- P202: Added 32-bit LEA through both effective-address sizes.
- P203: Added task-register state, reset, load, and snapshot contracts.
- P204: Added protected-mode LTR and STR task-register instructions.
- P205: Added 32-bit TSS privilege-stack entry for higher-privilege interrupt gates.
- P206: Added 32-bit protected-mode IRET return to a lower-privilege stack.
- P207: Added an end-to-end 32-bit TSS interrupt and IRET privilege round-trip regression.
- P208: Added 32-bit MOVZX and MOVSX byte and word destination forms.
- P209: Added 32-bit address-size override to dword MOVZX and MOVSX forms.
- P210: Added 32-bit BSF and BSR operand-size forms.
- P211: Added operand-size-overridden LGDT and LIDT with full 32-bit descriptor-table bases.
- P212: Added SGDT and SIDT with full six-byte 80386 descriptor-table stores.
- P213: Added 80386 CR0, CR2, and CR3 transfer forms, including noncanonical MOD handling.
- P214: Added the active S3 execution record to keep CPU work evidence-driven and bounded by whole-machine checkpoints.
- P215: Added protected-mode POP ES, POP SS, and POP DS through the existing segment descriptor loader.
- P216: Added the CPL-zero 16-bit protected-mode POPF path with high EFLAGS preservation.
- P217: Added the ModR/M memory form of LMSW through the existing machine-status state boundary.
- P218: Added CPL-zero CLTS with a dedicated CR0 task-switched state transition.
- P219: Added operand-size-overridden 32-bit protected-mode far JMP.
- P220: Added same-privilege 16-bit protected-mode far CALL frames.
- P221: Added same-privilege 16-bit protected-mode memory far CALL frames.
- P222: Added same-privilege 16-bit protected-mode far RET frames.
- P223: Added same-privilege 32-bit protected-mode memory far CALL frames.
- P224: Added 32-bit protected-mode memory far JMP transfers.
- P225: Added bounded protected-mode PUSHFD and POPFD behavior.
- P226: Recorded a bounded selected-ROM trace through project-owned memory and CPU execution.
- P227: Added 32-bit immediate signed multiply forms through both address sizes.
- P228: Added 32-bit two-operand signed multiply through both address sizes.
- P229: Added single-operand 32-bit signed multiply into EDX:EAX.
- P230: Added the remaining 32-bit Group 3 arithmetic forms, including
  memory-address-size coverage and divide-error delivery.
- P231: Added signed byte and word Group 3 multiply and divide through their
  implicit AX and DX:AX register pairs.
- P232: Added protected-mode error-code frame support and `#GP(0)` delivery
  for nonzero-CPL CLI and STI.
- P233: Routed nonzero-CPL CLTS and control-register transfers through `#GP(0)`
  before their state transitions.
- P234: Added default DS:SI LODSW with direction-aware word indexing.
- P235: Routed ring-3 POPF and POPFD through `#GP(0)` before stack consumption.
- P236: Routed ring-3 LTR through `#GP(0)` before descriptor lookup or TR changes.
- P237: Routed ring-3 LGDT, LIDT, and LMSW through `#GP(0)` before operand access.
- P238: Recorded the owner-authorized CS-default execution-size correction and
  its bounded context, migration, verification, and non-goal contracts.
- P239: Added a project-native execution-context decoder with focused defaults,
  prefix-boundary, stack-width, and instruction-start tests; no instruction
  family claims 32-bit-default support yet.
- P240: Routed MOV immediate and register push/pop through the shared context,
  including independent SS stack addressing; ModR/M and string families remain
  pending and are not implied by this slice.
- P241: Routed the `89` and `8B` ModR/M MOV forms through context-selected
  operand and address widths; no broader ModR/M or string-family claim is made.
- P242: Routed unprefixed `LODSB` and `LODSW/LODSD` through context-selected
  data and SI/ESI widths; repeated string and segment-override paths remain
  explicitly outside this slice.
- P243: Routed non-repeated MOVS and STOS through context-selected data and
  SI/ESI/DI/EDI widths; repeat and segment-override string paths remain pending.
- P244: Routed non-repeated CMPS and SCAS through context-selected data and
  SI/ESI/DI/EDI widths; REP/REPNE comparison behavior remains pending.
- P245: Routed REPE and REPNE CMPS/SCAS through context-selected count, data,
  and address widths; repeat MOVS/STOS and segment overrides remain pending.
- P246: Routed REP MOVS/STOS through context-selected count, data, and address
  widths; F2 transfer-prefix and segment-override combinations remain pending.
- P247: Routed accumulator-immediate ALU forms through context-selected word or
  dword operands; ModR/M immediate families remain separate migration work.
- P248: Routed register INC and DEC through context-selected word or dword
  operands while preserving the existing carry-flag behavior.
- P249: Routed CBW/CWDE and CWD/CDQ through context-selected operand widths.
- P250: Routed immediate PUSH forms through context-selected data and stack
  address widths.
- P251: Routed PUSHA/POPA through context-selected register and stack widths.
- P252: Routed near relative JMP through context-selected displacement and EIP
  widths.
- P253: Routed near CALL and RET through context-selected target, return-data,
  and stack-address widths.
- P254: Routed two-byte near conditional jumps through context-selected
  displacement and IP/EIP widths.
- P255: Routed LEA through context-selected effective-address and destination
  register widths.
- P256: Routed immediate ModR/M MOV through context-selected data and address
  widths.
- P257: Routed LOOP-family count selection through context-selected address
  width while preserving code-segment target width.
- P258: Recorded the owner priority clarification: finish all M2 T2 execution,
  ROM-trace, coverage, and PCjs-comparison gates before requesting T3.
- P259: Routed ModR/M word and dword ALU forms through execution context with
  independent CS-selected operand/address sizes and focused default-32 coverage.
- P260: Added the M2 T2 80386 coverage matrix, separating implemented work from
  partial paths and defining the evidence required before T3 can be requested.
- P261: Routed `81`/`83` ModR/M immediate ALU forms through execution context
  with independent operand, address, and immediate widths.
- P262: Routed byte ModR/M ALU forms through context so `67` selects their
  16-bit or 32-bit effective-address decoding without changing byte width.
- P263: Routed byte Group 1 immediate ALU (`80`) through context with shared
  byte flags and context-selected ModR/M addressing.
- P264: Routed ModR/M XCHG through context with byte, word, dword, and both
  effective-address widths while preserving flags.
- P265: Routed accumulator moffs loads and stores through context-selected
  operand and address widths.
- P266: Routed PUSHF/POPF through context so flags data width and SS stack
  address width remain independent.
- P267: Routed MOVZX/MOVSX through context with independent destination and
  ModR/M address widths across the two-byte opcode path.
- P268: Routed BSF/BSR through context with independent operand and ModR/M
  address widths, preserving zero-source destinations, ZF, and instruction
  length.
- P269: Routed two-operand IMUL through context with independent signed data
  and ModR/M address widths, preserving CF/OF overflow behavior and length.
- P270: Routed immediate IMUL (`69`/`6B`) through context with independent
  signed immediate/data and ModR/M address widths, preserving CF/OF and length.
- P271: Routed `SETcc` through context with byte data and context-selected
  ModR/M address widths, preserving condition evaluation and instruction length.
- P272: Routed `POP r/m` through context with independent operand, ModR/M
  address, and SS stack-address widths, preserving post-pop destination timing.
- P273: Routed `PUSH r/m` through context with independent operand, ModR/M
  address, and SS stack-address widths, preserving pre-push source timing.
- P274: Routed accumulator-register `XCHG` through context with CS-selected
  word or dword width, preserving flags and instruction length.
- P275: Routed `BOUND` through context with independent signed data and ModR/M
  address widths, preserving vector-five fault restart behavior.
- P276: Routed `MOV Sreg, r/m16` and `MOV r/m16, Sreg` through context with
  fixed selector data and context-selected ModR/M address widths.
- P277: Routed short `Jcc rel8` through context with CS-selected IP/EIP target
  width, preserving all existing EFLAGS condition predicates.
- P278: Routed `LEAVE` through context and corrected context stack word access
  so SS 32-bit addressing remains independent from a 16-bit operand width.
- P279: Added protected-mode `ARPL` through context with RPL/ZF semantics,
  both ModR/M address widths, and an explicit real-mode invalid-opcode fault.
- P280: Added LDTR state plus protected-mode `SLDT` and `LLDT`, and connected
  active LDT resolution to project-owned protected segment loading.
- P281: Routed `0F 00` system selector instructions through the shared execution
  context, preserving fixed 16-bit selector data while selecting ModR/M address
  decoding from the active address size.
- P282: Routed the implemented `0F 01` descriptor-table and machine-status-word
  forms through context-selected operand and ModR/M address sizes.
- P283: Routed implemented CR0, CR2, and CR3 transfer forms through the default-32
  execution context while preserving their fixed 32-bit data and existing
  privilege behavior.
- P284: Routed `CLTS` through the default-32 execution context while preserving
  its CPL-zero fault path and prefix-inclusive instruction length.
- P285: Routed FS/GS push and pop forms through context-selected operand sizes
  and the existing independent SS stack-address boundary.
- P286: Routed `0F 80` through `0F 8F` near conditional jumps through
  context-selected IP/EIP target and displacement widths.
- P287: Added project-native 32-bit double-shift flags and routed SHLD/SHRD
  through context-selected operand and ModR/M address widths.
- P288: Routed LSS/LFS/LGS through context-selected pointer data and ModR/M
  address widths while retaining existing segment validation.
- P289: Routed register-index BT/BTS/BTR/BTC forms through context-selected
  operand and ModR/M address widths, including memory bit-index expansion.
- P290: Routed the `0F BA` immediate BT/BTS/BTR/BTC subgroup through the shared
  contextual bit-test path.
- P291: Added 32-bit logical-shift flags and routed Group 2 SHL/SHR `C1/D1/D3`
  forms through context-selected operand and address sizes.
- P292: Added arithmetic-right-shift flags and routed Group 2 SAR `C1/D1/D3`
  forms through context-selected operand and address sizes.
- P293: Added 32-bit rotate-left flags and routed Group 2 ROL `C1/D1/D3`
  forms through context-selected operand and address sizes.
- P294: Added rotate-right flags and routed Group 2 ROR `C1/D1/D3` forms
  through context-selected operand and address sizes.
- P295: Recorded the owner-authorized NXVM CPU authority correction: NXVM CPU
  coverage and execution become primary, while Intel IA-32 resolves semantic
  conflicts and PCjs remains the PC/AT and whole-machine reference.
- P296: Audited NXVM's five post-80386 opcode entries. Each is an explicit
  `UndefinedOpcode()` TODO handler, so M2 requires their `#UD` behavior rather
  than unsupported later-processor functionality; added the family-level NXVM
  CPU instruction manifest.
- P297: Routed NXVM's explicit `WBINVD`, `WRMSR`, `RDMSR`, `CPUID`, and `RSM`
  TODO opcodes through vector 6, preserving the faulting EIP with and without
  an execution-size prefix.
- P298: Routed Group 2 RCL/RCR `C1/D1/D3` forms through contextual operand and
  address widths, including `66`, `67`, carry propagation, and defined
  one-bit overflow behavior.
- P299: Routed NXVM Group 2 `/6` forms for `C0/C1/D0/D1/D2/D3` through vector
  6, with focused fault-EIP coverage for every encoding.
- P300: Corrected the existing byte RCR flag path to use RCR-specific one-bit
  overflow semantics while preserving overflow for larger counts.
- P302: Routed all defined C0 byte Group 2 forms through the execution context,
  including carry rotations, multi-bit shifts, and 67-selected memory access.
- P303: Reused the contextual byte Group 2 helper for D0 and D2, correcting the
  prior byte RCR carry result while adding memory and address-size coverage.
- P304: Corrected contextual byte Group 2 zero-count behavior and preserved OF
  where multi-bit byte SHL/SHR/SAR leave it undefined.
- P305: Routed F7 Group 3 word and dword forms through the execution context,
  correcting historical 66/67 test assumptions and covering both CS defaults.
- P306: Routed FE byte INC/DEC through the execution context, including
  67-selected memory addressing under a default-32 code segment.
- P307: Routed F6 byte Group 3 through the execution context, including
  default-32 execution and 67-selected memory addressing.
- P308: Added contextual FF /0 and /1 INC/DEC forms with independent 66 and
  67 operand and address-size coverage.
- P309: Routed FF /2 near calls through the execution context with independent
  operand data width and SS stack-address width.
- P310: Routed FF /4 near jumps through the execution context at both operand
  widths.
- P311: Added contextual REP/REPNE LODS with context-selected data and address
  widths.
- P312: Routed XLAT through context-selected BX/EBX table addressing.
- P313: Routed ENTER through the execution context with independent operand and
  SS stack-address widths.
- P314: Routed segment-overridden moffs through the execution context while
  retaining conservative fallback for other segment-overridden families.
- P315: Routed segment-overridden REP LODS through the contextual source-string
  path without widening other segment-overridden instruction families.
- P316: Routed segment-overridden REP MOVS through the contextual source-string
  path while retaining the fixed ES destination contract.
- P317: Routed non-repeated segment-overridden LODS through the contextual
  source-string path without intercepting REP forms.
- P318: Routed segment-overridden REP CMPS through the contextual source-string
  path while retaining the fixed ES comparison operand.
- P319: Routed same-privilege protected-mode IRET through contextual frame
  widths while preserving the existing cross-privilege path.

## S5: Implement I/O, Reset, Stepping, And Traces

- P8: Closed S5 after project-native dispatch, reset, bounded stepping, and
  trace evidence reached the first unmapped ROM port without a device hack.

- P7: Routed the selected-ROM trace through the rebuilt machine core and record
  deterministic unmapped-port stop events without synthetic device responses.

- P6: Added rebuilt machine composition for physical memory, port dispatch,
  reset, bounded stepping, and unified CPU/port trace hooks.

- P5: Added a width-aware project-native rebuilt port dispatcher with explicit
  unmapped-port errors, independent read/write ownership, and trace events.

- P1: Added a project-owned 8-bit I/O port bus with separate directional
  registration, explicit unmapped failures, and optional structured trace
  events. Concrete devices, reset wiring, and wider accesses remain active.
- P2: Added a typed instruction-trace wrapper that snapshots CPU state before
  and after execution without changing instruction semantics.
- P3: Added a deterministic bounded CPU stepper that composes the existing
  memory, port-I/O, and trace boundaries without host scheduling assumptions.
- P4: Added a project-owned PC/AT 386 core composition and verified reset-vector
  execution through high ROM mapping, physical memory, CPU, and stepping.

## S6: Differential CPU Validation

- P21: Matched real-mode byte and word accumulator moffs loads/stores through
  one `A0-A3` program with changed-RAM comparison.

- P20: Matched real-mode NOP, accumulator XCHG, CBW/CWD, and SAHF/LAHF through
  one generic program; WAIT and far CALL remain separate.

- P19: Matched real-mode TEST, XCHG, general MOV, and LEA through one generic
  `84-8D` program; segment and POP r/m forms remain separate.

- P18: Matched real-mode Group One immediate register ADD/ADC/SBB/SUB/CMP
  forms through the generic dispatcher; logical AF remains EXC-002 scoped.

- P17: Matched all real-mode `70-7F` short conditional encodings through one
  zero-displacement program and per-instruction EIP comparison.

- P16: Matched real-mode INSB/OUTSB through declared ports, RAM deltas, and
  instruction-by-instruction I/O journals.

- P15: Normalized rebuilt RAM deltas to changed bytes and matched real-mode
  `60-61` plus immediate stack/IMUL `68-6B` programs against PCjs.

- P14: Added passing generic real-mode numeric programs for selected `00-3F`
  arithmetic, complete `40-5F`, and all `B0-BF` immediate-register encodings.

- P13: Promoted the S6 exception list to the project-wide compatibility
  register, resolved the logical-AF conflict as EXC-002, and recorded the owner
  default to retain NXVM-aligned CPU behavior without per-case approval.

- P12: Recorded the real-mode logical-AF PCjs mismatch; resolved as EXC-002.

- P11: Established the sole S6 compatibility-exception register and recorded
  owner-approved EXC-001: real-mode `D6` retains NXVM/80386 `#UD` despite the
  pinned PCjs oracle's no-fault behavior.

- D6 conflict: PCjs advanced real-mode `D6` without vector six while the
  NXVM-aligned rebuilt CPU delivered `#UD`; resolved as EXC-001.

- P8: Aggregated PCjs debugger prefix steps into one logical instruction and
  matched operand-size-prefixed 32-bit DX I/O against rebuilt execution.

- P7: Generalized test-only declared ports and matched 16-bit DX IN/OUT access
  without coupling output observation to an invented input value.

- P6: Added test-only I/O journals and matched configured 8-bit IN/OUT access
  direction, port, width, and value through both CPU execution paths.

- P5: Converted the harness to generic program lockstep at the shared rebuilt
  dispatcher boundary; each iteration captures pre/post state and RAM delta.

- P4: Added independent PCjs RAM-image changed-byte deltas and matched a
  real-mode `MOV moffs, AL` store; unchanged-value write tracing remains P5+
  work.

- P3: Added an executable isolated PCjs CPU oracle and real-mode lockstep
  baseline for NOP, immediate MOV, ADD, and DEC; broader state and side-effect
  comparison remains active P4+ work.

- P2: Replaced the PCjs-device-proxy proposal with the owner-authorized
  one-instruction PCjs CPU differential gate; preserved the later standalone
  browser DOS workload as a mandatory native-machine M2 completion gate.

- P1: Recorded the CPU-free temporary PCjs proxy inventory and project-owned
  memory/ports/IRQ/DMA/reset/time bridge contract for the S6 harness.

- P1 is retired design evidence only; it must not be used as a device-runtime
  implementation plan.

## P362 `CC-CD-CE-CF` Interrupt Control Checklist

Implemented project-native INT3, INT, INTO, and same-privilege IRET delivery
through IVT/IDT gates. Verified real-mode and 32-bit protected-mode frames,
operand-size handling, flag effects, and a software-gate privilege rejection.
Privilege stack switching and external interrupt admission remain pending.

## P363 `D6-D8-DF` Undefined Opcode Checklist

Implemented NXVM-equivalent vector-6 delivery for D6 and D8-DF. Tests verify
all nine encodings use the real-mode IVT and preserve the faulting EIP. No FPU
behavior was synthesized; protected fault completion remains pending.

## P367 `F4-FA-FB` Processor Control Checklist

Implemented HLT, CLI, and STI with protected CPL/IOPL authorization. Focused
tests cover halt state, IF mutation, and same-privilege #GP(0) delivery.

## P368 `F1` Undefined Opcode Checklist

Added NXVM-equivalent vector-6 delivery for F1 and focused fault-EIP coverage.

## P369 `F0` LOCK Prefix Checklist

Added shared LOCK validation and memory-bus atomic execution for supported RMW
forms; invalid forms now deliver #UD.

## P370 `8F /0` POP r/m Checklist

Implemented POP r/m with 16/32-bit data widths, ModR/M memory addressing, and
undefined-extension delivery.

## P371 `9B` WAIT Checklist

Implemented WAIT with CR0.TS-controlled vector-7 delivery and fault-EIP tests.

## P372 `9C-9D` Flag Stack Checklist

Implemented PUSHF/POPF real/non-VM protected paths with width and privilege
masks; v86 remains pending state-model work.

## P373 `62` BOUND Checklist

Implemented signed memory-bound checking with vector-5 and vector-6 paths.

## P374 `P373` Gate Correction

Reformatted the P373 test and reran the complete fail-fast gate successfully.

## P375 `63` ARPL Checklist

Implemented protected selector RPL adjustment, ZF results, and real-mode #UD.

## P376 `6C-6F` String I/O Checklist

Implemented project-native INS/OUTS with REP element boundaries and port-bus
access.

## P377 `A4-A7-AA-AF` Generic String Checklist

Implemented MOVS, CMPS, STOS, LODS, and SCAS with operand/address-size
selection, source overrides, DF, and per-element REP/REPNE behavior.

## P378 `0F A0-AF` Extended Checklist

Implemented NXVM-covered FS/GS, bit-test, double-shift, IMUL, far-pointer,
and MOVZX forms through the rebuilt CPU; undefined A-family encodings fault.

## P379 `0F BA-BF` Extended Checklist

Implemented NXVM-covered immediate bit operations, BTC, BSF/BSR, and MOVSX
through the rebuilt extended-instruction module.

## P380 `0F 01` System-Table Checklist

Implemented rebuilt SGDT/SIDT/LGDT/LIDT/SMSW/LMSW with operand-size and CR0.PE
semantics; task-state forms remain separate active work.

## P381 `0F 00` System-Selector Checklist

Implemented rebuilt LDTR/TR state and NXVM-covered selector operations with
protected-mode admission, selector stores, GDT validation, and ZF verification.

## P382 `0F 20-23` Control/Debug Checklist

Implemented project-native CR0/CR2/CR3 and defined DR transfer forms.

## P383 `0F 02-03, 06` System Access Checklist

Implemented rebuilt LAR, LSL, and CLTS with GDT descriptor access checks,
operand-size-selected results, ZF signaling, and CPL-zero CLTS authorization.

## P384 `0F 24, 26` Test-Register Checklist

Implemented NXVM-covered TR6/TR7 register transfers with fixed dword width,
CPL-zero admission, and `#UD` for invalid encodings.

## P385 Explicit NXVM Undefined-Extension Checklist

Implemented vector-six routing for the explicit NXVM undefined `0F` extensions
without masking unrelated incomplete opcode families.

## P386 `0F 00 /2,/3` Privilege Checklist

Corrected rebuilt LLDT/LTR CPL-zero admission and verified protected `#GP(0)`
delivery without selector-state mutation.

## P387 `0F 00 /4,/5` Verification Checklist

Corrected rebuilt VERR/VERW type and CPL/RPL/DPL verification behavior to
match NXVM's selector-validation flow.

## P388 Segment-Loader Hidden-CPL Checklist

Corrected shared data/code segment-loader privilege checks to use CS cached DPL
instead of selector RPL; direct protected-loader regression coverage added.

## P389 LDT Descriptor-Lookup Checklist

Added project-native TI/LDTR descriptor lookup for shared segment and selector
verification paths, with an active-LDTR data segment regression.

## P390 LDT System-Instruction Execution Checklist

Verified LAR, LSL, and VERR execute active-LDTR TI selector lookups through the
rebuilt CPU.

## P398 Virtual-8086 Baseline Checklist

Implemented NXVM-style v86 segment, code-size, stack-size, and paging-user
semantics. Interrupt, I/O-permission, and v86 flag virtualization remain
explicit protection dependencies.

## P399 `30-3D` XOR/CMP Checklist

Closed rebuilt XOR/CMP opcode-form evidence with byte/word/default-32/66/67
coverage and explicit CMP non-writeback checks.

## P400 32-bit TSS Privilege Stack Checklist

Implemented 32-bit TSS ring-zero interrupt stack switching, outer IRET return,
and LTR busy-state recording; v86 and 16-bit TSS paths remain open.

## P401 Virtual-8086 Interrupt Frame Checklist

Implemented and verified 32-bit v86 interrupt entry and IRET restoration through
the rebuilt TSS stack path.

## P402 Virtual-8086 Flag Stack Checklist

Implemented NXVM-style v86 PUSHF/POPF IOPL admission and #GP frame delivery.

## P403 Virtual-8086 Processor-Control Checklist

Corrected rebuilt v86 CLI/STI admission to use CPL three and verified both the
IOPL-three state changes and low-IOPL #GP TSS-frame delivery.

## P404 Virtual-8086 Software-Interrupt Checklist

Implemented and verified NXVM-style low-IOPL v86 #GP delivery for INT3, INT
imm8, and triggered INTO before interrupt-gate lookup.

## P405 Decimal-Adjust State Checklist

Corrected rebuilt DAA/DAS high-digit adjustment to use post-low-adjust state;
focused NXVM-distinguishing DAS coverage added.

## P406 F6/F7 Fault-Delivery Checklist

Converted rebuilt Group Three divide faults and `/1` undefined encoding from
host errors into architected #DE/#UD vector delivery with faulting EIP frames.

## P407 FE/FF Undefined-Extension Checklist

Converted unsupported FE/FF extensions and register far-pointer forms into
rebuilt #UD delivery without changing valid Group Four/Five execution.

## P408 Explicit Primary Undefined-Opcode Checklist

Routed NXVM-table-defined `82` and `0F B8/B9` through rebuilt #UD delivery.

## P409 Group Two Undefined-Extension Checklist

Converted C0/C1/D0-D3 `/6` from a host error to rebuilt #UD delivery.

## P410 External-Interrupt Admission Checklist

Added IF-gated rebuilt external-interrupt admission and HLT wakeup through the
existing project-native event path.

## P411 Fault-Delivery Escalation Checklist

Added rebuilt contributory/page-fault double-fault escalation, secondary-fault
delivery for benign cases, and an explicit triple-fault terminal signal.

## P412 `84-8F` Undefined-Encoding Checklist

Routed invalid LEA, segment-MOV, and LES/LDS encodings through rebuilt #UD
delivery instead of host exceptions.

## P413 AAM And C6/C7 Fault Checklist

Routed zero-base AAM and invalid C6/C7 extensions through rebuilt #DE/#UD
delivery instead of host exceptions.

## P414 Maskable-Interrupt Inhibition Checklist

Added rebuilt one-instruction IRQ inhibition after POP SS, MOV SS, and STI.
