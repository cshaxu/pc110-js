# M2 T3: TypeScript Core PC/AT Hardware

## S1: 8259A-Compatible PIC Variants

- P1: Recorded the project-native master/slave 8259A execution plan, selected
  PC/AT ports, cascade boundary, sources, verification, and non-goals.
- P2: Added the project-native single-8259A ICW/OCW, fixed-priority, mask,
  IRR/ISR, EOI, and vector-acknowledgement state core.
- P3: Composed native master/slave PICs with IRQ2 cascade arbitration, vector
  acknowledgement, and four explicit 8-bit PC/AT port-bus ranges. CPU INTR
  admission remains P4.
- P4: Registered the native PIC in the rebuilt machine core and delivered
  pending vectors only at CPU-accepted boundaries, including HLT and STI
  inhibition behavior.
- P5: Added and manually checked a browser-native CPU/PIC reset-state
  checkpoint. S1 is complete; no firmware or DOS workload is claimed.

## S2: 8253/8254-Compatible PIT And Speaker Timing

- P1: Recorded the native PIT plan, PC/AT port and wiring evidence, strict
  T3 S5 speaker-gate boundary, verification matrix, provenance, and stops.
- P2: Added a standalone binary 8254 counter model with control, latching,
  modes 0-5, gate transitions, read-back, and explicit tick output edges.
- P3: Mapped native PIT ports, explicit PIT advancement, counter-0 IRQ0 into
  the native PIC/CPU path, and counter-2 output observation without a host clock.
- P4: Added and manually verified browser-visible native PIT reset outputs.
  S2 is complete; no running clock, speaker gate, sound, firmware, or DOS claim.

## S3: 8237-Compatible DMA And Page Registers

- P1: Recorded the native DMA controller/page-register plan, cascade and port
  evidence, FDC boundary, verification matrix, provenance, and stop conditions.
- P2: Added standalone 8237 register, grant, terminal-count, priority,
  mask/request, auto-init, decrement, and byte/word addressing state behavior.
- P3: Registered native DMA0/DMA1 and selected page-register ports in the
  rebuilt machine, including DMA1 even-port and reset behavior.
- P4: Added DMA1 channel-4 cascade arbitration and generic grant-driven memory/
  endpoint transfer behavior without an FDC or storage implementation.
- P5: Added and manually verified browser-native DMA reset masks. S3 is
  complete; no requesting device, FDC, storage, firmware, or DOS claim.

## S4: RTC/CMOS And Selected-Machine Configuration

- P1: Recorded the project-native MC146818-compatible RTC/CMOS plan, selected
  PC/AT `0x70`/`0x71` and IRQ8 evidence, deterministic-time boundary, and the
  deferred S5 NMI boundary. No runtime behavior changed.
- P2: Added an isolated deterministic RTC/CMOS state model with calendar,
  BCD/binary and 12/24-hour forms, SET, periodic/update/alarm status,
  status-C acknowledgement, and checksum behavior. Ports, IRQ8, NMI, host
  time, firmware, storage, and browser behavior remain outside this part.
- P3: Mapped native RTC/CMOS ports and explicit tick advancement into the
  rebuilt machine. Enabled local events raise PIC IRQ8; address bit 7 remains
  a deferred S5 NMI signal only. No host clock or firmware behavior exists.
- P4: Added explicit checksum-backed CMOS configuration input and recorded the
  M1 DeskPro 4MB base/extended-memory declaration as a named variant input.
  Generic PC/AT defaults remain unmodified; DeskPro wiring remains separate.
- P5: Added and manually verified browser-native RTC reset status and NMI-mask
  checkpoint. S4 is complete; no running clock, NMI delivery, firmware, media,
  storage, or DOS claim.

## S5: System Ports, NMI, Reset, And A20 Glue

- P1: Recorded the project-native `0x61`, RTC NMI-mask, reset, A20, and S6
  8042 ownership plan. The selected source does not establish synthetic `0x92`
  behavior; generic and DeskPro-specific boundaries remain explicit.
- P2: Added isolated generic `0x61` timer-2/speaker gates, refresh observation,
  and parity/I/O-check control state. Port mapping, PIT wiring, NMI, reset,
  A20, 8042 details, DeskPro errors, and host audio remain outside this part.
- P3: Mapped native `0x61` I/O and connected bit 0 to PIT counter-2 gate and
  bit 5 to its output signal. Speaker output remains a local signal only; NMI,
  8042, A20, reset, host audio, and DeskPro lines remain deferred.
- P4: Added native vector-2 NMI admission and delivery, bypassing IF and STI
  inhibition while respecting the RTC address mask. Hardware NMI sources,
  8042, A20, reset, and DeskPro-specific error lines remain deferred.
- P5: Added a narrow 8042 output-port A20/reset contract. A20 controls native
  physical memory and reset pulse resets CPU state only; I/O protocol and
  keyboard-controller behavior remain T3 S6 work.
- P6: Added and manually verified browser-native system-port/PIT2/A20 reset
  state. S5 is complete; no host audio, keyboard, firmware, media, storage,
  display, or DOS claim.

## S6: 8042 Keyboard Controller And Keyboard Boundary

- P1: Recorded the selected project-native 8042 controller plan: `0x60`/`0x64`
  ownership, command/status/output-buffer behavior, command-byte IRQ1 gate,
  raw keyboard-byte ingress, S5 output-port boundary, deterministic timing,
  validation, and exclusions. No runtime behavior changed.
- P2: Added isolated selected-8042 command/data/status state with a one-byte
  output buffer, controller response families, command-byte keyboard gating,
  raw-byte ingress eligibility, output-port/reset-pulse results, and focused
  tests. Port mapping, native IRQ1 wiring, output-port effects, browser input,
  firmware, storage, display, and DOS remain outside this part.
- P3: Mapped native byte-wide `0x60`/`0x64`, routed accepted interrupt-enabled
  raw keyboard bytes to PIC IRQ1, and forwarded output-port/reset-pulse results
  to existing S5 machine contracts. `0x61`, scan-code translation, browser
  mapping, firmware, storage, display, and DOS remain outside this part.
- P4: Verified rebuilt-machine controller buffer, IRQ1, output-port A20, and
  self-test reset behavior. The selected-ROM trace still stops after two
  instructions at unrelated unclaimed port `0x84` (`F000:F907`); no synthetic
  controller or firmware response was added.
- P5: Added and manually verified browser-native reset-state 8042 status,
  command-byte, output-buffer, and keyboard-clock observations. S6 is
  complete; browser key mapping, firmware, storage, display, and DOS remain
  outside this subtask.

## S7: BIOS POST Checkpoints Through Real Device Paths

- P1: Recorded the selected-ROM POST-checkpoint plan. The first missing write
  at `F000:F907` is source-classified as retained spare DMA page-register
  `0x84` state, not an ignored diagnostic port. Future trace advances require
  real device ownership, focused tests, ledger evidence, and no synthetic
  firmware response.
- P2: Added all selected spare DMA page registers as retained byte-wide native
  state, independent from channel page registers and reset to zero. This makes
  the `0x84` DeskPro POST checkpoint a real device path, not a trace-only sink.
  The selected ROM now reaches `F000:F94F` after 34 instructions and stops at
  the next classified option-ROM read boundary `0xE0000`.
- P3: Added explicit selected-machine floating-bus `0xFF` reads and ignored
  writes while preserving strict physical-memory defaults. The selected ROM now
  reaches `F000:F9B6` after 63 instructions and stops at unclassified I/O write
  `0xF1`; no synthetic port response was added.
