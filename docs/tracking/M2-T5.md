# M2 T5: TypeScript Video, Input, And Platform Presentation

## S1: Selected VGA Hardware And Video Memory

- P1: Recorded the selected IBM VGA plan after the ROM reached MDA-compatible
  mode port `0x3B8`. Compatibility state, VGA registers, video memory, and
  browser presentation are separated; no diagnostic sink, BIOS/DOS shortcut,
  font, or renderer was added.
- P2: Added the complete retained MDA compatibility CRTC/mode/status port
  family as project-native VGA-visible state. The selected ROM reaches
  `F000:BB36` after 224 instructions and stops at CGA compatibility port
  `0x3D8`; no MDA renderer, printer, or VGA main-register behavior is claimed.
- P3: Added the complete retained CGA compatibility CRTC/mode/color/status
  family as project-native VGA-visible state. The selected ROM reaches
  `F000:BB44` after 231 instructions and stops at VGA attribute-controller
  port `0x3C0`; no CGA renderer, framebuffer, or VGA main-register behavior is
  claimed.
- P4: Added the retained native VGA Attribute Controller, including its
  index/data flip-flop, palette gating, all palette/control register classes,
  and status-one reset. The selected fixed-budget ROM trace completes 1,000
  instructions at `F000:9C05`; no renderer, framebuffer, video-memory plane,
  or successful boot is claimed.
- P5: Added the complete retained VGA sequencer bank and opt-in selected-ROM
  trace budget/tail diagnostics. The extended trace reaches the ROM's
  `F000:BBB4` PIT counter-zero POST failure loop. A proposed per-instruction
  PIT clock was rejected before commit because it produced premature IRQ0;
  later scheduling must use CPU cycles, not instruction counts.
- P6: Added project-native step-cycle accounting and exact fractional PIT
  scheduling, plus the selected early-80386 `MOV CR` MOD-field compatibility
  behavior. The native trace now crosses the real protected-mode far jump to
  `0018:87AA` and returns to `F000:87DC`; full 80386 timing and other device
  clock domains remain explicit `TODO(High)` work.
- P7: Exposed native PIT channel-1 refresh state at PC/AT `0x61` bit 4 without
  altering timer-2/speaker ownership. The selected trace advances beyond the
  ROM refresh loop to `F000:B5F7`; audio and complete scheduler behavior remain
  outside this part.
- P8: Added the physical-memory hardware-aperture contract required by native
  VGA memory. Device windows observe A20-normalized addresses, overlay writable
  RAM only, and cannot overlap immutable ROM or another device. No VGA plane,
  renderer, or browser presentation is claimed.
- P9: Added and composed the complete retained VGA Graphics Controller register
  family at `0x3CE`/`0x3CF`, with defined index masks and reset behavior. The
  native plane/latch aperture remains the following part.
- P10: Composed native A0000-BFFFF VGA planar memory with four planes, latches,
  window decoding, addressing modes, and Graphics Controller read/write modes.
  Browser presentation remains separate.
- P11: Transferred shared `0x3D4/0x3D5` ownership to native VGA CRTC state while
  retaining CGA mode/color/status compatibility ports. CRTC display-start and
  cursor values are available to later presentation.
- P12: Added native Miscellaneous Output and 256-entry DAC state, including
  6-bit RGB palette transfers and address advancement, for later presentation.
- P13: Added a browser canvas fed by a native text framebuffer projection of
  VGA planes, CRTC display start, and DAC palette state. It is not DOS boot or
  complete VGA rendering evidence.
- Browser runtime audit: native FDC/DMA exists, but no browser-owned, validated
  DeskPro ROM or floppy asset contract exists yet. The M1 PCjs reference media
  path is explicitly excluded from the M2 runtime.
- P14: Added selected local-media descriptors, SHA-256 validation, native ROM
  alias mapping, native FDC floppy attachment, and browser-run-loop gating.
  User-provided files remain required; neither PCjs nor host paths are runtime
  asset sources.
- P15 evidence: validated local floppy attached to native FDC does not advance
  the ROM beyond its PIT polling loop at `F000:C668`; timer behavior remains
  the next whole-machine blocker.
- P16: Replaced selected generic CPU timing fallbacks with explicit
  compatibility classes for stack, return, exchange, and short-jump paths. The
  one-million-instruction native trace improved PIT0 count from 27596 to 9644,
  but did not demonstrate an FDC command or boot.
- P17: Bound native ROM diagnostic event retention to the requested tail and
  optional CS-transfer events so multi-million-instruction traces remain a
  fixed-memory execution diagnostic; no machine behavior changes.
- P18: Added opt-in, bounded native CS:EIP hit counts to distinguish repeated
  ROM delay execution from an unreachable control-flow exit. Three million
  native instructions hit the delay loop 415 times with `ECX=0x11EA`, proving
  its roughly `0x1389` outer iterations decrease normally; no semantics change.
- P19: A bounded forty-million-instruction native trace reached six real delay
  returns at `F000:C67E`, later programmed PIT2, and remained in subsequent ROM
  delay work. No FDC command or boot is claimed.
- P20: Extended the bounded ROM watch with post-instruction CS:EIP so the next
  native delay return identifies its real caller without altering execution.
  The eight-million-instruction check records `C67C -> C644`; a full delay
  return still requires the longer native budget.
- P21: Added a selected-machine non-faulting physical-hole and unpopulated-I/O
  profile. Its original `0xFF` physical-read interpretation was superseded by
  P109's controlled PCjs lockstep evidence; ignored physical writes remain.
- P22: Increased browser host instruction batching and throttled presentation
  refresh without changing native CPU/device time, improving real ROM startup
  throughput while retaining responsive browser controls. Manual native browser
  execution stayed running at `F000:B5EE` and paused responsively at `F000:C672`.
- P23: Added browser-only Set-1 make/break mapping and a bounded delivery queue
  to the existing native 8042 ingress, keeping DOM input outside device cores.
- P24: Added the selected M1 IBM VGA option ROM as a separately validated local
  browser input and mapped it natively at `0xC0000`; no PCjs firmware bytes or
  runtime dependency were added.
- P25: Added native little-endian 16-bit transactions for all indexed VGA,
  MDA, and CGA register pairs, retained the full five-bit attribute index bank,
  and extended the diagnostic trace with the selected VGA ROM. The bounded
  trace now executes the option ROM through `C000:01FB` without a device fault.
- P26: Manually ran the browser-native machine with all three validated local
  media files through `C000:030F`; it remained running before a responsive
  user pause. This is browser execution evidence, not a DOS-boot claim.
- P27: Removed full CPU snapshots from the untraced execution hot path while
  preserving trace snapshots and cycle behavior. The selected browser run
  reached `C000:069C` after approximately twelve seconds and paused cleanly.
- P28: Added native VGA Input Status 0 SWSENSE and Feature Control port
  families required by the IBM VGA ROM DAC probe. The five-million-instruction
  trace passes that probe and returns to system ROM; the later 40M service
  loop remains an explicitly unclassified whole-machine comparison target.
- P29: Reclassified the observed `INT 10h` to `INT 6Dh` transfer as normal IBM
  VGA service flow and made ordinary bounded ROM regression runs opt out of
  per-instruction snapshots unless a diagnostic tail, transfer log, or watch
  is requested. The 80M native run completes naturally at `F000:C24B`; no
  new device boundary or boot claim is made.
- P30: Ran a 160M-instruction Fast Execution native regression after the RTC
  and FDC scheduling closures. It completes at adjacent ROM helper address
  `F000:C24A` without an error or new device boundary; no POST, boot, or DOS
  claim is made.
- P31: Added the project-native selective diagnostic trace selector and replay
  identity. Fast Execution remains trace-free; selective mode emits requested
  instruction boundaries while retaining fault evidence and machine I/O/stop
  boundaries. Full Debug remains opt-in through existing tail or transfer
  diagnostics. No opcode, device, firmware, or boot behavior changed.
- P32: Owner corrected the trace workflow after an interrupted long Selective
  Trace run exposed avoidable snapshot cost. Long Fast runs are milestone-only;
  normal work uses focused tests, differential checks, and short checkpoints.
  A mismatch or unexpected boundary triggers a short Full Debug replay from a
  verified checkpoint.
- P33: Audited the project-native checkpoint boundary. Existing snapshots are
  observational only; an honest restorable checkpoint must include CPU hidden
  state, writable memory/A20, scheduler remainders, every composed device,
  pending input/NMI, and replay identity. CPU/RAM-only restoration is rejected
  as insufficient for whole-machine diagnostic replay.
- P34: Added paired capture/restore contracts for rebuilt CPU state and
  physical writable memory/A20. CPU restoration includes the precise
  interrupt-inhibit boundary count and hidden architectural state; memory
  restoration excludes immutable ROM and device ownership. This is a verified
  checkpoint foundation, not an entire-machine checkpoint claim.
- P35: Added capture/restore for virtual clock time and the exact PIT, RTC,
  and FDC-DMA fractional remainders. A replay can now resume the same native
  timing phase after CPU/memory restoration; device-state restoration remains
  required before an entire-machine checkpoint claim.
- P36: Decoupled bounded machine-event diagnostics from instruction snapshots
  and replaced the synthetic per-instruction MDA status toggle with native
  CPU-cycle-driven horizontal and vertical retrace windows. A bounded 5M Fast
  run now crosses IBM VGA ROM polling and returns to `F000:C679`; its next
  observed boundary is native PIT polling/programming, not a VGA fault.
- P37: Recorded the required persistent-output rule after one bounded Fast
  milestone process completed but its detached terminal result was unavailable.
  The run is not treated as endpoint evidence and is not repeated; future
  milestone runs must retain an identity-bearing external diagnostic log.
- P38: Aligned browser-native composition with the selected DeskPro trace by
  enabling its secondary PIT at `0x48-0x4B`. Browser floating I/O now applies
  only to genuinely unpopulated ports; the checkpoint test verifies native
  ownership of `0x4B` and floating behavior at adjacent `0x4C`.
- P39: Manually verified browser-native execution with validated local system
  ROM, VGA ROM, and floppy after P36/P38. It advanced from `F000:C675` to
  `F000:C67A` during a short run and paused responsively at `F000:C672` with
  native PIT/RTC/PIC state visible. The text canvas remained blank; no boot,
  DOS, or display-success claim is made.
- P40: A further bounded browser-native run crossed the previous PIT-delay
  region from `F000:C672` through `F000:C246` and paused at `F000:DCA7`.
  Native execution is therefore still advancing beyond the prior benchmark
  path. The canvas remains blank and FDC DMA remains inactive.
- P41: Refined trace governance after the previous long-run cost finding.
  Milestone Fast execution now records only replay identity, sparse verified
  checkpoints, and an observed boundary. Detailed transient instruction state
  is collected only by a short Full Debug replay after a mismatch or unexpected
  boundary; routine work remains short tests, differential checks, and short
  ROM checkpoints.
- P42: Added read-only native FDC checkpoint fields for phase, main status,
  IRQ, pending DMA bytes, and drive-zero readiness/cylinder. This gives a short
  browser checkpoint sufficient storage evidence without retaining a long
  instruction trace or changing FDC execution behavior.
- P43: Corrected the stale S6 status record. The browser now owns a native
  CPU/memory/device composition with validated user-provided ROM and floppy
  inputs; it has firmware-progress evidence, but no DOS claim until the floppy
  reaches a visible prompt.
- P44: Classified `F000:DCA6` as the DeskPro BIOS keyboard-buffer wait, not a
  storage or video fault. The canvas is now focusable and status reports the
  current CPU address rather than incorrectly labeling it a reset address.
- P45: Corrected P44's next-step assumption. The observed controller state has
  keyboard clock disabled, so a raw browser key cannot be assumed to reach the
  BDA queue. The next bounded diagnostic must classify the real `0x60`/`0x64`
  exchange and controller-enable transition before any keyboard-peripheral
  behavior is implemented.
- P46: Added optional project-relative persistent output for the native ROM
  trace runner. A bounded Fast smoke run wrote its identity and final boundary
  to the requested evidence file; this is diagnostic retention only and does
  not enable long trace capture or change emulation behavior.
- P47: Added an independent bounded port-event tail for Fast ROM diagnostics.
  It preserves only the requested latest port accesses, so timer IRQ activity
  cannot displace the keyboard-controller exchange needed for the next
  whole-machine classification.
- P48: The first governed 160M keyboard-port attempt was interrupted before a
  terminal result and left no valid evidence, so it was not repeated. The trace
  runner now creates its identity-bearing log before execution and appends
  bounded diagnostic output as it occurs; a streaming smoke run passed.
- P49: Corrected checkpoint delivery order to match the owner-approved
  browser-first workflow. Complete device restore is an on-demand prerequisite
  for a real bounded mismatch replay, not speculative work before the next
  native browser or short-checkpoint finding.
- P50: Added a browser-native bounded tail of sixteen project-native port
  accesses while explicitly disabling instruction tracing. A paused firmware
  checkpoint can now show the controller exchange immediately before a wait
  without a long Node replay or any guest-visible instrumentation.
- P51: Reclassified the late-arriving P48 persistent output as valid milestone
  evidence: the 160M Fast run with the selected floppy ends at `F000:DCA7` and
  reaches the `0x64`/`0x60` controller exchange. Its legacy port format lacks
  data values, so P50's value-bearing short browser checkpoint is the next
  required evidence; the long diagnostic is not repeated.
- P52: Refined future Fast port-tail output to include zero-padded port,
  access width, and value. The bounded smoke run records `03D9/8 30` and
  `0084/8 12`; P48 is preserved as historical boundary evidence and is not
  rerun.
- P53: Added an owner-authorized local-development media control, visible only
  with `?dev-media=1` on the Vite development server. It uses environment-only
  file paths and the existing asset hashes; default and production runtime
  paths do not contain host media locations or automatic media loading.
- P54: Added the minimal native AT keyboard power-on boundary. When the 8042
  releases its data and clock lines, it emits BAT `0xAA` through the existing
  output buffer and IRQ1 path. Keyboard command protocol remains explicit
  `TODO(High)` work; no BIOS, BDA, or guest-service behavior was added.
- P55: The single governed P54 Fast validation still ends at `F000:DCA7` after
  160M instructions. BAT is therefore not sufficient to satisfy the observed
  BDA keyboard-queue wait. The next diagnostic must use an atomic checkpoint
  and bounded replay; no second full Fast run or speculative keyboard behavior
  is authorized by this result.
- P56: Added reversible state contracts for the PIC cascade, 8042 controller,
  AT keyboard BAT state, and 8042 output port. Tests prove capture, mutation,
  restore, and equal IRQ/byte continuation. This is an input/interrupt slice,
  not yet a whole-machine checkpoint claim.
- P57: Added reversible timing/DMA/system-control state for PIT counters,
  both DMA controllers and page registers, RTC/CMOS tick phase and address,
  and port `0x61`. Focused continuation tests pass; FDC, video, serial, and
  remaining machine state are still outside this partial checkpoint.
- P58: Added reversible FDC and floppy state, including command/result/IRQ
  queues, in-flight DMA bytes, drive position, and writable media bytes.
  Restore republishes the DMA request without inventing an IRQ; video, serial,
  fixed storage, CPU, RAM, and machine composition remain pending.
- P59: Added reversible DeskPro secondary-PIT and FPU-control observation
  state. The selected firmware's secondary timer path now has deterministic
  continuation coverage; video, serial, fixed storage, and machine-level
  composition remain pending.
- P60: Added reversible COM1/COM2 UART and LPT1 state, including FIFOs,
  register state, and pending interrupt selection. Restore is transport-free
  and does not synthesize PIC input; fixed storage, video, and machine-level
  composition remain pending.
- P61: Added reversible fixed-disk controller and media state, including
  in-flight PIO buffers, CHS/register state, IRQ selection, and writable raw
  media. Restore requires the attached-drive topology to match and creates no
  new IRQ; video and machine-level composition remain pending.
- P62: Added reversible native VGA state: MDA/CGA timing and compatibility
  registers, VGA register banks, DAC progress, feature/miscellaneous output,
  four planar memory banks, and latches. A cross-component restore test passes;
  only machine-level checkpoint composition remains pending.
- P63: Composed all project-native CPU, RAM, scheduler, I/O, storage, video,
  optional DeskPro timer, and pending-NMI state into one in-memory core
  checkpoint. Exact component equality and NMI/instruction continuation pass;
  outer input queue and replay-identity composition remain next.
- P64: Added an in-memory diagnostic replay session that binds the atomic core
  checkpoint to queued browser keyboard bytes, virtual instruction position,
  and exact project/PCjs/media/configuration identity. It rejects identity
  changes and provides no serialization or product save-state API.
- P65: Added a zero-allocation instruction-boundary runner for reaching one
  explicit diagnostic checkpoint. It creates a CPU snapshot only on a matched
  boundary, so Fast execution remains free of routine trace or state capture.
- P66: Connected the atomic checkpoint to the selected-ROM runner. A Fast
  `CS:EIP` boundary capture can now start one bounded Full Debug replay in a
  newly composed native core; two equal replay completions are required before
  its trace is accepted as deterministic evidence.
- P67: Ran the single governed Fast checkpoint to `F000:DCA7` and two equal
  5,000-instruction native replays. The observed loop is the DeskPro BIOS BDA
  keyboard-read wait with no headless key event, not evidence for a synthetic
  keyboard response; browser IRQ1-to-BDA delivery is the next check.
- P68: Added the selected native AT keyboard command slice: `0xED` LED setup
  and `0xF4`/`0xF5`/`0xF6` scan control now return through the 8042 output
  buffer and IRQ1 path. The change preserves physical command-byte line gates;
  browser revalidation is required before claiming the BIOS wait is resolved.
- P69: Extended the existing development status readout with PIC masks and
  keyboard scan admission. It is observational only and makes the browser
  keyboard-boundary diagnosis distinguish masked IRQ1 from missing input.
- P70: Implemented the selected ROM's AT keyboard reset exchange: `0xFF`
  produces ordered `0xFA` then `0xAA` through the one-byte 8042 output buffer.
  Pending response state is checkpointed; browser evidence is still required.
- P71: Corrected the ROM-evidenced 8042 keyboard-data clock release. The
  selected ROM sends keyboard commands before `0xAE`; direct port `0x60`
  traffic now releases `NO_CLOCK` before native keyboard response admission.
  Focused and full gates pass; browser revalidation remains required.
- P72: Corrected 8042 keyboard-line admission and added a development-only
  browser `Send A` control. Scan-code admission follows the released clock,
  while BAT still requires both lines; the control reuses the product Set-1
  queue and native ingress, is absent outside `?dev-media=1`, and has no
  guest-memory or firmware shortcut.
- P73: Added a fixed-size development status tail for only the native 8042
  ports `0x60` and `0x64`. It preserves the ROM command sequence after general
  video/timer traffic overwrites the ordinary port tail; no machine behavior
  changes.
- P74: Added a selected DeskPro 8042 interface-test profile result. The generic
  PC/AT result remains `0x00`; the selected ROM receives its observed `0x05`
  result through explicit machine configuration, without input or firmware
  shortcuts.
- P75: Corrected the browser keyboard diagnosis. At the selected wait the
  command byte is `0x5D`, retaining `NO_CLOCK`; queued host scan codes are
  correctly not admitted. The precise guest-visible completion condition is
  recorded separately by P76; no synthetic F1 input is introduced.
- P76: Added read-only BDA keyboard-buffer head/tail observation to the native
  browser status. The selected `F000:DCA6` loop compares these pointers after
  `INT 15h AH=90h AL=02h`; this records whether native input reaches guest RAM
  without adding an input or firmware shortcut.
- P77: Added the selected DeskPro RAM layout as an explicit native profile:
  640 KiB low RAM, 1 MiB at `0x100000`, and 384 KiB relocatable RAM at
  `0xFA0000`. Browser and reference-ROM construction now share it; generic
  PC/AT memory remains unchanged.
- P78: Added the standard keyboard ACK for the selected ROM's `0xF2` probe.
  The response traverses the existing 8042 output buffer and IRQ1 path; no
  synthetic BIOS response or keyboard identity payload is introduced.
- P79: Corrected the selected 8042 interface-test configuration. PCjs generic
  success is `0x00`, which the selected ROM routes through keyboard reset and
  enable; the earlier `0x05` override selected a different error branch and
  has been removed from browser and ROM-trace construction.
- P80: Added a bounded writes-only 8042 diagnostic tail for Fast Execution.
  It separates controller commands from routine status polling without retaining
  instruction snapshots or changing machine behavior.
- P81: Modelled the 8042's one-status-poll controller-response publication
  boundary. Controller replies become OBF-visible after one status poll, while
  keyboard bytes remain immediate; snapshots are pure observation.
- P82: Added an opt-in, bounded PCjs 8042 transaction probe on the local
  `pc110` branch. It records actual controller transaction ordering and caller
  addresses without changing emulation behavior; a 50M Fast native run reached
  `F000:DCA7` without the assumed `F000:CEDB` checkpoint.
- P83: Added an explicit diagnostic reference-server mode for the local PCjs
  `pc110` branch. Normal reference mode remains pinned; probe mode injects the
  opt-in ChipSet setting only into temporary XML and was verified over local
  HTTP. Browser transaction capture remains pending because this environment
  blocks the local reference URL.
- P84: Corrected probe execution evidence. Diagnostic mode now selects PCjs
  uncompiled execution and its regenerated `2.25` bundle containing the probe,
  rather than merely serving an instrumented source module. The normal pinned
  reference path remains unchanged.
- P85: Added a verified developer-media launcher. It exports only pinned PCjs
  ROM bytes to ignored temporary files, validates the local floppy, and feeds
  the existing opt-in browser development endpoints; product runtime media
  ownership remains unchanged.

## S5: Additional Selected Devices

- P1: Planned complete native COM1 16550 behavior after the selected DeskPro
  ROM reached IIR port `0x3FA`. No UART port behavior is added in this part.
- P2: Added a project-native COM1 16550 register model with DLAB aliases,
  FIFO state, interrupt priority, line/modem status, loopback, transmit
  observation, and native IRQ4 composition. The selected ROM crosses `0x3FA`
  and next stops at COM2 IIR `0x2FA`; COM2 remains the next same-family part.
- P3: Composed the same native UART model as COM2 at `0x2F8`-`0x2FF` with
  IRQ3. This is separate machine wiring, not a duplicated serial device; the
  selected ROM trace is the follow-up acceptance evidence.
- P4: Added selected-machine floating/ignored behavior for unpopulated I/O,
  preserving strict port-bus defaults for focused tests. This matches the
  BIOS probe of absent `0x3BC`; the configured LPT at `0x378` remains a
  separate native-device requirement.
- P5: Added the configured native LPT1 register family at `0x378`-`0x37A`,
  including reset state, status/control semantics, ACK-driven IRQ7, and a
  host-independent transmit observation boundary.

## S6: Structured Trace, Status, And Error Presentation

- P86: Added an opt-in same-origin PCjs diagnostic surface beside the native
  browser machine. Shared verified resource handling serves only a temporary
  probe-enabled reference machine in development; the in-app browser's blank
  XML/XSL iframe remains an environment limitation, not a PCjs mismatch.
- P87: Replaced the direct XML iframe with PCjs's supported HTML embedding API
  and corrected the diagnostic XSL parameter path. The temporary XSL now passes
  `pc110Probe:true` through its chipset whitelist; the live bounded 8042 tail
  is exported read-only for paired diagnosis.
- P88: Corrected the native 8042 reset status to keep the distinct
  security-inhibit state clear. Focused and browser evidence now align with the
  PCjs controller-test transaction sequence `10, AA, 18, 19, 55`.
- P89: Preserved the native 8042 output-data latch after OBF consumption.
  The selected controller-test flush now returns the retained `0x55`, matching
  the paired PCjs transaction sequence without adding firmware behavior.
- P90: Owner authorized a bounded whole-machine controlled-lockstep diagnostic.
  Its plan keeps the product core native while requiring PCjs-equivalent
  observable state and first-difference reporting for short replay windows.
- P91: Added the opt-in PCjs paused instruction-step control on its `pc110`
  branch. It uses PCjs's ordinary positive-budget CPU path, reports virtual
  cycles, and is verified in the same-origin diagnostic wrapper.
- P92: Migrated PC110JS to npm-only tooling. `package-lock.json` is now the
  sole dependency lock; pnpm metadata and commands were removed from active
  repository guidance and scripts.
- P93: Added a diagnostic-only native lockstep adapter with normalized CPU and
  virtual-cycle snapshots. Native instruction, IRQ, NMI, and halt boundaries
  are explicit; PCjs snapshot expansion and coordination remain pending.
- P94: Expanded the opt-in PCjs `pc110` lockstep snapshot with CPU registers,
  segment caches, control/debug state, and descriptor tables. It is read-only
  diagnostic state; field normalization and cross-machine coordination remain
  pending.
- P95: Added a project-owned CPU snapshot comparator that returns the first
  established architectural difference deterministically. Browser sampling,
  equivalent entry state, and device-event comparison remain pending.
- P96: Bound the development-only native page and same-origin PCjs iframe to
  the controlled comparator. It refuses unmatched or running entry states;
  equivalent reset construction and selected device journals remain pending.
- P97: Verified the direct same-origin PCjs browser wrapper loads the opt-in
  version-two lockstep control and live BIOS transaction state. The automation
  proxy cannot inspect the nested iframe, so no cross-machine result is claimed.
- P98: Added native normalized observation for PIC, PIT, DMA, 8042, and RTC at
  a diagnostic CPU boundary. PCjs device export and device-level comparison
  remain the next separate part.
- P99: Added the matching opt-in PCjs selected-device snapshot on its `pc110`
  branch and made reference assets require it. Device-field comparison and an
  equivalent whole-machine entry point remain pending.
- P100: Added first-difference comparison for selected PIC, PIT, DMA, 8042,
  and RTC observations after CPU state. Natural ROM checkpoint equivalence and
  bounded event journals remain pending.
- P101: Added paired normal reset controls to the diagnostic coordinator. The
  browser-only reset boundary compares real post-reset snapshots without
  copying PCjs reset ordering or synthesizing device state.
- P102: Matched PCjs's pre-ICW 8259 reset state instead of preloading PIC
  registers. The browser reset boundary now reaches the next PIT zero-count
  encoding difference.
- P103: Added the missing native-adapter regression assertion for P102 after
  correcting a local PowerShell exit-code masking error; published `main`
  history was preserved with a follow-up verification commit.
- P104: Normalized native PIT zero-count snapshots to PCjs's `65536` diagnostic
  encoding while retaining the project-owned 8254's raw internal register state.
- P105: Required verified native ROM/VGA/floppy media before browser instruction
  comparison, preventing blank-memory execution from being misreported as a CPU
  difference.
- P106: Projected the native 8042's existing delayed controller-output state to
  PCjs's `OUTBUFF_DELAY` diagnostic observation, after a short ROM window first
  differed at status `0x18` versus `0x118`.
- P107: Added pre/post CS:EIP, EFLAGS, and virtual-cycle boundaries to accepted
  lockstep steps so the first EFLAGS mismatch can be replayed without adding a
  separate trace framework.
- P108: Fixed the selected DeskPro differential RTC seed to `1990-01-01T00:00:00`
  on both native and PCjs machine configurations. This removes host-clock input
  from short lockstep replay. The part also corrected the stale browser
  checkpoint test to the established pre-ICW PIC reset observation; it is not
  a timer shortcut or runtime RTC behavior change.
- P109: Corrected selected DeskPro unallocated physical reads from `0xFF` to
  PCjs-compatible zero after the controlled replay reached `F000:F94F` and
  compared `CMP word ptr [E000:0000],55AAh`. This is a profile-only memory
  policy correction; generic strict-memory behavior and ignored hole writes
  remain unchanged.
- P111: Added an owner-authorized, opt-in PCjs physical-memory diagnostic
  probe after reset replay exposed contradictory `C8000`/`E0000` observations.
  It reports only direct reads for those addresses and does not modify PCjs
  memory, reset, CPU, device, timing, or normal runtime behavior.
- P112: Disabled PCjs auto-start only in the local diagnostic wrapper so the
  coordinator obtains a paused pre-execution state before its first normal
  reset. That cold-state probe established `C8000=FF` and `E0000=FF`, so it
  also corrects P109's soft-reset inference: selected physical holes float as
  `0xFF` before ROM execution.
- P113: Corrected the diagnostic fixed-RTC injection from the machine root to
  the selected `<chipset>` element, which is the PCjs XSL parameter source.
  This removes host-time input from cold controlled replay.
- P114: Registered the volatile PCjs RTC Status-C latch as EXC-004 for
  diagnostic snapshots only. Guest reads remain compared through CPU state.
- P115: Added PCjs lifecycle observability to the local diagnostic wrapper so
  controlled replay starts only after the asynchronous reference initialization
  has settled.
- P116: Added a read-only PCjs PIT timing observation contract for the first
  stable channel-one count difference at `F000:BB13`.
- P117: Retained both lockstep boundaries on an entry difference. The first
  PIT mismatch is now evidenced at `F000:BB15`: native `1188` cycles versus
  PCjs `857` cycles, with PCjs PIT1 started at cycle `852`.
- P118: Aligned native I/O instruction timing from 12 to PCjs's selected
  80286-table value of 5 cycles. Replay crossed `F000:BB15`; the next first
  difference is at `F000:BB19`, where PIT reload phase remains distinct.
- P119: Replaced the shared primary-PIT fractional phase with reload-relative
  per-counter phase state, captured in project-native machine checkpoints.
  Replay crossed `F000:BB19`; the next difference at `F000:BB26` follows a
  native `849` versus PCjs `882` CPU-cycle gap.
- P120: Moved all selected device settlement to the following instruction
  boundary while preserving immediate CPU virtual-cycle accounting. Replay
  crossed `BB1B`, `BB19`, and `BB26`; the next difference is PIT0 at `BB29`
  after native `856` versus PCjs `889` CPU cycles.
- P121: Recorded native and PCjs cycle charges on each successful lockstep
  boundary to locate the first timing-table difference directly. The reset
  vector's `EA` far jump is the first difference: native 7 versus PCjs 11.
- P122: Corrected the project-native timing classification for far jump and
  far call, preserving the distinct PCjs-compatible 11- and 13-cycle charges.
  Cold browser replay now reports the reset-vector `EA` as 11 cycles on both
  machines with a matched architectural boundary.
- P123: Added a development-only, bounded 16-instruction controlled-lockstep
  window. It records timing-only differences and stops at the first CPU or
  device difference; browser evidence localizes the remaining PIT0 difference
  to accumulated timing before `F000:BB27`, not the equal-cycle `B0` opcode.
- P124: Corrected generic conditional-control timing to use complete
  fall-through lengths, with PCjs-compatible Jcc 7/3 and LOOP 8/4 charges and
  zero prefix charge. The repeated loop/Jcc window differences are gone; the
  remaining PIT0 divergence still follows earlier non-control timing classes.
- P125: Added read-only ModR/M timing shape and covered observed accumulator,
  group, move, compare, test, control, and CLI timing forms without address
  rules. Thirteen browser windows now match all CPU cycles through `BB27` at
  887 cycles; the remaining first difference is PIT0 phase only.
- P126: Corrected generic 8254 mode-3 visible double-counting and odd-period
  carry. Browser lockstep crosses the former `BB27` PIT0 boundary; the next
  first difference is CPU EAX at `BB3A` with both machines at 914 cycles.
- P127: Aligned native MDA `0x3BA` status timing and reserved bits with the
  selected 16 MHz PCjs profile. Focused tests pass, and the bounded browser
  lockstep window again reports 16 matched boundaries with matched timing.
- P128: Aligned native VGA color Input Status 1 (`0x3DA`) timing, diagnostic
  bits, and checkpoint state with the selected PCjs profile. Bounded browser
  lockstep crosses `BB3E`; the next independent boundary is PIT1 output at
  `F000:9BF5` with matched timing.
- P129: Aligned mode-2 PIT terminal/reload phase, `MOV Sreg,r/m16`, and REP
  string first/subsequent timing with observed PCjs behavior. Thirty-two
  bounded browser windows match with no timing or device difference.
- P130: Split `MOV Sreg,r/m16` timing by retained ModR/M form after bounded
  replay showed the register form costs two PCjs cycles and the memory form
  costs three. This replaces P129's over-broad three-cycle classification. The
  same replay corrected the selected PIT integer clock from `1193182` to the
  PCjs-defined `1193181`, removing an early mode-3 tick at `F000:9C05`.
  A cold replay then completed 1,152 matched instruction boundaries.
- P131: Made browser port-tail retention an explicit `trace-ports=1`
  development diagnostic. Fast Execution no longer formats or retains every
  PIT polling transaction, while targeted diagnostic sessions retain the same
  bounded native port tail.
- P132: Replaced the selected machine's internal bigint clock and fractional
  scheduler hot path with exact safe-integer arithmetic while preserving
  bigint checkpoint contracts. The bigint recurrence remains a focused test
  oracle; the bounded ROM benchmark improved from about 928 ms to 743 ms and
  a fresh PCjs lockstep window remains matched.
- P133: Added explicit safe-integer numerator guards to the scheduler and PIT
  public APIs. Normal instruction charges retain P132's exact recurrence;
  only unrepresentable one-call clock products are rejected.
- P134: Removed clone-heavy segment-cache reads from the internal executor and
  segmented-memory paths while preserving public copying semantics. Fast
  execution also skips unused tracepoint allocation; the bounded ROM benchmark
  improved from about 743 ms to 721 ms.
- P135: Replaced the PC/AT PIT cycle path's transient per-counter edge arrays
  with a project-native boolean edge result while retaining the general 8254
  observation API. Focused PIT regressions pass and the bounded ROM benchmark
  improved from about 721 ms to about 712 ms.
- P136: Added a development-only ordinary F1 Set-1 input control for a
  reproducible early-input browser check. The queued input does not bypass the
  selected BIOS keyboard wait: native execution still reaches `F000:DCA6` with
  command byte `5D` and an empty BDA keyboard buffer.
- P137: Added an opt-in bounded PCjs `pc110`-branch history of changed 8042
  command bytes. It preserves the transition sequence across a normal reference
  run without altering PCjs execution and is exposed only on the diagnostic
  reference page for the next native comparison.
- P138: Included the PCjs saved-state restore transition in that opt-in 8042
  command-byte history. The prior recorder correctly covered runtime writes but
  missed the first restored byte, which is the boundary needed by this boot
  diagnosis.
- P139: Added the matching bounded native 8042 command-byte transition history
  to the existing development checkpoint. It records only real command-byte
  changes and is checkpointed for deterministic short replay; it does not
  change controller, keyboard, or firmware behavior.
- P140: Expanded the existing diagnostic page's read-only PCjs 8042 event view
  from its display tail to the probe's already bounded event history. This
  permits correlating command-byte transitions with the preceding real port
  transactions without changing PCjs execution or probe retention.
- P141: Restored the selected DeskPro CPU hole-read policy to zero after a
  fresh controlled lockstep again reached `F000:F94F`. The cold direct-memory
  probe remains diagnostic evidence only; the PCjs CPU-visible `CMP` result is
  the behavioral authority for this execution path.
- P142: Reverted P141 after a clean restart and paired reset reproduced the
  same `F000:F94F` instruction with PCjs EFLAGS `0x86`, proving its CPU read is
  `0xFFFF`. The prior opposite sample was not an equivalent reference state;
  floating reads remain the selected behavior.
- P143: After P142, a clean local server and fresh paired browser reset
  completed 64 controlled windows, or 1,024 instruction boundaries, with
  matched CPU state, selected devices, and virtual timing. This is bounded
  lockstep evidence only; no long replay or DOS-boot claim is made.
- P144: Corrected P77's selected DeskPro extended-RAM size from `0x100000` to
  the M1 reference contract's `0x300000` at `0x100000`, restoring the selected
  4 MiB machine map. Focused aperture-boundary coverage preserves low and
  relocatable RAM behavior. A clean browser rerun retains the independent
  `F000:C24B`/`0x5D` keyboard path, so no boot-progress claim is made.
- P145: Added the owner-approved browser resource budget. Ordinary verification
  reuses one Codex browser tab; a second is permitted only for an active,
  bounded native-versus-PCjs comparison, and unused tabs are closed before
  CPU-intensive checks. No emulator behavior changed.
- P146: Consolidated source authority and M2 convergence governance: NXVM
  supplies M2 CPU structure and scope without its platform hacks; PCjs supplies
  observable PC/AT behavior and profile evidence without its JavaScript runtime;
  PC110-EMU supplies future PC110-specific supporting evidence without its
  hardcoded boot workarounds. Whole-machine diagnosis now requires bounded
  batch comparison followed by first-difference replay.
- P147: Added an opt-in, bounded PCjs batch lockstep operation and matching
  project-native batch coordinator. It compares before/after boundaries without
  retaining an intermediate trace, then resets, replays the matched prefix, and
  single-steps only the first mismatched batch. A browser run from selected
  local media matched reset and a 1024-instruction batch at 3434 cycles per
  endpoint. Normal PCjs execution and the product runtime remain unchanged.
- P148: Raised only the diagnostic reset-to-first-difference search limit to
  65536 instructions for the existing selected-ROM blocker. Each PCjs batch
  remains capped at 1024 instructions and the normal product runtime remains
  unchanged.
- P149: Corrected first-difference narrowing so a per-instruction virtual
  cycle mismatch stops replay before a later device state exposes it. The
  selected-ROM P148 result showed this is required for exact timer diagnosis.
- P150: Corrected the native 80386 timing estimator for the complete
  accumulator arithmetic-immediate opcode family. The first selected-ROM
  timing mismatch was `AND AL, 07h` at `F000:BBC6`: native charged two cycles
  while PCjs charged three.
- P151: Removed PCjs's explicit `ramLow test="false"` warm-boot shortcut from
  the diagnostic-only machine XML. The shortcut writes `0x1234` to BDA
  `0040:0072` to skip ROM memory tests and is not valid M2 hardware behavior.
- P152: Classified valid `0F 01` Group 7 descriptor-table and machine-status
  timing forms by opcode and ModR/M role. The cold diagnostic's first remaining
  timing difference was the memory `LGDT` path at `F000:8796`; no ROM-address
  exception or PCjs runtime behavior was introduced.
- P153: Classified all valid early-80386 `0F 20/22` control-register transfer
  timing forms by opcode and CR index, preserving their historical MOD-field
  compatibility. The cold diagnostic crossed `LGDT` and localized the next
  timing difference to `MOV EAX,CR0` at `F000:879C`.
- P154: Classified the valid memory `FF /5` far-jump timing form from its
  opcode and ModR/M extension. The cold diagnostic crossed `MOV EAX,CR0` and
  localized the next timing difference to the selected ROM's memory far jump
  at `F000:87A5`; no ROM-address rule was introduced.
- P155: Distinguished protected-mode `MOV Sreg,r/m16` from the existing
  real-mode timing form. The estimator receives the pre-instruction mode and
  applies the generic descriptor-load timing contribution without a selector
  or firmware-address rule.
- P156: Added a profile-owned port `0x61` refresh-status signal. Generic PC/AT
  wiring still uses PIT counter 1, while the selected DeskPro composition uses
  the documented virtual CPU-cycle bit without changing CPU execution.
- P157: Classified the complete STOS string family separately from the generic
  string fallback. Non-REP byte/word/dword stores use the PCjs selected
  three-cycle class; the existing REP first/subsequent scheduling remains
  explicit.
- P158: Recorded one governed 1,000,000-instruction native Fast checkpoint
  after the 65,536-boundary cold lockstep match. The selected floppy profile
  reaches IBM VGA option-ROM execution at `C000:020B`; no boot or DOS claim is
  made.
- P159: Restored the documented raw-diagnostic boundary by ignoring local
  `artifacts/verification/raw/` files. Identity-bearing Fast logs remain local
  evidence and cannot enter source commits or distribution artifacts.
- P160: Reconstructed the cold 65,536-boundary baseline and matched one
  further 1,024-instruction batch at 4,441 virtual cycles on both endpoints.
  This is bounded continuation evidence, not a DOS or complete-state claim.
- P161: Ran the native browser machine with validated local media to
  `F000:C660`. The FDC was ready but idle; the pinned ROM classifies this as
  existing PIT delay/calibration execution, not a storage or input blocker.
- P162: Extended the governed reset-to-first-difference diagnostic limit to
  131,072 instructions after native browser POST reached `F000:C242` while
  the M1 PCjs workload reaches `A:\\>`. Batches remain 1,024 instructions.
- P163: Corrected generic mode-3 fractional phase rebasing at both output
  half-period transitions. The cold native-versus-PCjs search now matches all
  131,072 boundaries, including the former `F000:B5B7` PIT0 count difference.
- P164: Raised the single governed reset-to-first-difference search to 262,144
  instructions after the native browser remained at the known `F000:C24B`
  keyboard-buffer wait beyond P163's matched range. Batches remain 1,024.
- P165: Separated M1's warm-boot PCjs DOS proof from the no-shortcut M2 cold
  oracle. The cold diagnostic retains `ramLow test="true"`; M2 still requires
  a standalone native DOS prompt without the BDA warm-boot marker.
- P166: Replayed the verified 262,144-boundary cold prefix and advanced 64
  further 1,024-instruction batches. Native and PCjs remained equal through
  327,680 boundaries; this is bounded comparison evidence, not a DOS claim.
- P167: Replayed the cold prefix and advanced through 393,216 boundaries with
  equal normalized state and timing. The next governed comparison target is
  the existing one-million-instruction option-ROM checkpoint, not routine
  indefinite extension.
- P168: Raised the single cold reset-to-first-difference diagnostic limit to
  one million instruction boundaries for the documented option-ROM checkpoint.
  The batch size and mismatch replay mechanism remain unchanged.
- P169: Corrected the opt-in PCjs lockstep snapshot to normalize ESP through
  its public `getSP()` accessor. The first one-million-boundary discrepancy at
  `F000:A8D4` was a stale diagnostic field after `PUSH DX`, not native CPU
  behavior.
- P170: Added the selected DeskPro CMOS `0x10` floppy-drive byte through the
  generic RTC configuration contract. Its native browser and ROM-trace paths
  now declare the PCjs-observed `0x44` two-1.44MB-drive configuration.
- P171: Added the selected DeskPro CMOS `0x12` fixed-disk byte through the
  generic RTC configuration contract. The cold lockstep difference at
  `F000:B546` reads PCjs's Type 5 drive-zero value `0x50`; this declares profile
  hardware configuration only and does not mount or emulate a disk medium.
