# M1 T4 S6 Verification: Reference Quick Start

## Result

Pass.

## Environment

- Node.js `v24.14.0`
- pnpm `11.9.0`

## Procedure

The documented install, build, and local-run path was exercised from the
project checkout. The generated browser endpoint served the pinned reference
machine, and the local Edge proof reached `A:\>` with the documented controls.
The runner invokes pnpm consistently rather than depending on a separate npm
command on `PATH`.

The guide uses only sibling-relative prerequisites and does not claim M2
standalone behavior.
