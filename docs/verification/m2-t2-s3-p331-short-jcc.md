# M2 T2 S3 P331 Verification: Rebuilt Short Conditional Jumps

Focused tests cover every true and false condition, signed rel8 movement,
default-16 and default-32 EIP wrapping, and the `66` prefix length.

The full project gate passed: format, build, lint, `git diff --check`, and all
30 test files with 419 tests.
