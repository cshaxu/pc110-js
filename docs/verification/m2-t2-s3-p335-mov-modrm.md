# M2 T2 S3 P335 Verification: Rebuilt MOV ModR/M Family

Focused tests cover both transfer directions, byte registers, dword memory,
`66`, `67`, and segment override.

The full project gate passed: format, build, lint, `git diff --check`, and all
34 test files with 428 tests.
