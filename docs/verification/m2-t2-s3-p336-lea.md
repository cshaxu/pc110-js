# M2 T2 S3 P336 Verification: Rebuilt LEA

Focused tests cover effective-address wrapping, `66`, `67`, SIB, EIP length,
and rejection of register-only ModR/M.

The full project gate passed: format, build, lint, `git diff --check`, and all
35 test files with 431 tests.
