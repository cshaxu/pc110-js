# M2 T2 S3 P337 Verification: Rebuilt Accumulator XCHG

Focused tests cover all register exchanges, NOP, `66` dword selection,
instruction length, and EFLAGS preservation.

The full project gate passed: format, build, lint, `git diff --check`, and all
36 test files with 433 tests.
