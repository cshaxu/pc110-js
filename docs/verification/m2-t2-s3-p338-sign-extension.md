# M2 T2 S3 P338 Verification: Rebuilt Sign Extension

Focused tests cover negative sign extension in default-16 and `66` dword modes,
plus EFLAGS preservation.

The full project gate passed: format, build, lint, `git diff --check`, and all
37 test files with 435 tests.
