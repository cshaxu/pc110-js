# M2 T2 S3 P340 Verification: Rebuilt Immediate Register MOV

Focused tests cover every byte and word register encoding, `66` dword form,
little-endian values, and instruction length.

The full project gate passed: format, build, lint, `git diff --check`, and all
39 test files with 440 tests.
