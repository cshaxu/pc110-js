# M2 T2 S3 P341 Verification: Rebuilt Moffs MOV

Focused tests cover byte and dword loads/stores, DS and segment override,
`66`, `67`, EIP length, and little-endian data.

The full project gate passed: format, build, lint, `git diff --check`, and all
40 test files with 442 tests.
