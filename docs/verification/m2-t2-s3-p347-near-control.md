# M2 T2 S3 P347 Verification: Rebuilt Near CALL And JMP

Focused tests cover near and short relative targets, `66`, CALL stack push,
and SS D/B stack addressing.

The full project gate passed: format, build, lint, `git diff --check`, and all
46 test files with 456 tests.
