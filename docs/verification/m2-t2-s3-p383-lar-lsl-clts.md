# M2 T2 S3 P383 Verification: Rebuilt System Access Instructions

Focused tests verify LAR, LSL, and CLTS invalid-selector, GDT, system-type,
operand-size, instruction-length, and protected `#GP(0)` behavior. The full
project gate passed: format, build, lint, 73 test files / 553 tests, and
whitespace verification.
