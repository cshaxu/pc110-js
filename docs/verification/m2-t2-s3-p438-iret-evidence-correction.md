# M2 T2 S3 P438 Verification: IRET Evidence Correction

The rebuilt interrupt suite and full project gate passed after removing the
unreachable virtual-8086 guard. P437 remains limited to the focused protected
IRET `#GP(selector)` regression.
