# M2 T2 S5 P6 Verification: Rebuilt Machine Core

Focused tests verify a deterministic project-native rebuilt machine core resets,
steps to HLT, dispatches a registered port write, and emits CPU, port, and reset
trace events without using the legacy CPU. The full project gate passed.
