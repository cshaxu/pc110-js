# M2 T2 S6 P31 Verification: Browser-First Direction

The repository records the owner-authorized switch from numeric oracle-expansion
to browser-workload-first validation. It also records that the current browser
entry exposes MachineRuntime controls only and cannot claim native firmware,
storage, display, or DOS workload validation.
