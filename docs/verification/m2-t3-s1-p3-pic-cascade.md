# M2 T3 S1 P3 Verification: PIC Cascade

Focused tests verify master and slave vector routing, IRQ2 cascade propagation,
masking, paired EOI ordering, command-register reads, exported 8-bit port-bus
ranges, and invalid access rejection. The full repository gate is recorded by
the P3 commit.
