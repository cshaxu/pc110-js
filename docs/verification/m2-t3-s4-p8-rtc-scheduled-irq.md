# M2 T3 S4 P8 Verification

Focused rebuilt-machine coverage enables native RTC periodic interrupts, runs
one scheduled CPU instruction, and verifies RTC status C `0xC0` bits and slave
PIC IRQ8 request `0x01`. The full quality gate is required before commit.
