# M2 T4 S1 P5 Verification

Focused coverage verifies exact FDC service-slot carry, all controller data
rates, and an end-to-end CPU-cycle scheduled raw-sector DMA2 transfer into
physical memory with controller completion and IRQ6. The full quality gate and
selected ROM regression are required before commit.
