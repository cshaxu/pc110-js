# M2 T5 P115 Verification

- The reference asset test verifies the lifecycle fields are emitted by the
  local diagnostic page.
- Browser validation confirms the fields report a settled paused reference
  machine before a cold controlled reset.
- Full npm format, build, lint, test, and `git diff --check` gate is required
  before this part is committed.
