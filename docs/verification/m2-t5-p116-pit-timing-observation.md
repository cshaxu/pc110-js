# M2 T5 P116 Verification

- The PCjs source and generated uncompiled bundle expose identical read-only
  timing fields in the opt-in lockstep snapshot.
- The PC110JS reference-asset contract validates the required source marker.
- A settled browser replay reaches the existing PIT difference with timing
  evidence available for direct inspection.
- Full npm format, build, lint, test, and `git diff --check` are required
  before commit.
