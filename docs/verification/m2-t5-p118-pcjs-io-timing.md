# M2 T5 P118 Verification

- Focused estimator tests cover the PCjs-compatible I/O charge.
- The full npm format, build, lint, test, and `git diff --check` gate must pass.
- A settled browser lockstep replay must cross the prior `F000:BB15` PIT
  boundary or report the next first difference.
