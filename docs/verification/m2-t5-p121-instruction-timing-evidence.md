# M2 T5 P121 Verification

- Focused coordinator tests verify successful boundaries retain native and
  PCjs cycle charges with an equality marker.
- Full npm format, build, lint, test, and `git diff --check` are required.
- Browser replay must identify the first completed instruction whose cycle
  charge differs while architectural state remains equal.
