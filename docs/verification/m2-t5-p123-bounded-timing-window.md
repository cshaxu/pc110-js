# M2 T5 P123 Verification: Bounded Timing Window

- Focused coordinator tests retain cycle-only differences across a matched
  window and stop exactly at an execution-produced architectural difference.
- The browser development route exposes `Compare 16 boundaries` only with the
  existing PCjs diagnostic query parameters.
- Cold browser replay reports timing-only windows before stopping at the first
  PIT0 difference near `F000:BB27`.
- Full npm format, build, lint, test, and `git diff --check` are required.
