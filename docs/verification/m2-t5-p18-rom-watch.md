# M2 T5 P18 Verification: ROM Watch

The selected ROM trace reports explicit hit counts and the final `ECX` value
for the requested delay-return and next-test addresses while retaining bounded
trace state. The reported values are diagnostic evidence, not an emulation
shortcut.
