# M2 T5 P22 Verification: Browser Throughput

Build, lint, and tests verify the TypeScript application. Manual localhost
execution with validated local ROM and floppy remains running and accepts Pause
without a semantic exception.
