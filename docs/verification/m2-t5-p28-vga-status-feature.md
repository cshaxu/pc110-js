# M2 T5 P28 Verification: VGA Status And Feature

Focused tests cover DAC-dependent SWSENSE, byte-width rejection, Feature
Control masking, both write aliases, reset, and coexistence with the existing
MDA/CGA status-read aliases. The selected five-million-instruction trace
passes the VGA probe; the 40M trace is retained as negative evidence for the
remaining later service loop. The full quality gate remains required.
