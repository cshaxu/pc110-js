# M2 T5 P32 Verification

This is a documentation-only governance correction. `git diff --check` must
pass before commit. P31's focused CPU and machine trace tests remain the
implementation evidence for the existing diagnostic selector.
