# M2 T5 P38 Verification

The browser-native checkpoint test verifies the DeskPro secondary PIT exists,
owns `0x4B`, and leaves `0x4C` as floating selected-machine I/O. The full
quality gate is required before commit.
