# M2 T5 P56 Verification

- Focused tests cover capture, mutation, restore, exact state equality, PIC
  cascade continuation, and restoration of the pending keyboard BAT byte.
- Build and lint pass. Full gate and `git diff --check` are required before
  commit.
