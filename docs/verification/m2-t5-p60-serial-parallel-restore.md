# M2 T5 P60 Verification

- Focused tests prove capture, mutation, restore, exact equality, and UART
  receive/interrupt plus parallel status continuation.
- The full repository gate, including format, build, lint, test, and
  `git diff --check`, must pass before commit.
