# M2 T5 P61 Verification

- Focused tests prove capture, mutation, restore, exact equality, continued
  PIO reads, and writable raw-media restoration.
- The full repository gate, including format, build, lint, test, and
  `git diff --check`, must pass before commit.
