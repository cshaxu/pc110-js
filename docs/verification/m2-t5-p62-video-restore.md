# M2 T5 P62 Verification

- A focused cross-component test proves capture, mutation, restore, and exact
  equality for register banks, compatibility timing, planar memory, latches,
  DAC transfer progress, and output controls.
- The full repository gate, including format, build, lint, test, and
  `git diff --check`, must pass before commit.
