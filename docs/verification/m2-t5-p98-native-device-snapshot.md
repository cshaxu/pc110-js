# M2 T5 P98 Verification

- Focused adapter tests cover reset CPU and selected device observation state.
- The focused coordinator fixture was updated for the versioned snapshot
  contract and passes with the adapter tests.
- TypeScript build passes. PCjs device equivalence and a whole-machine
  comparison are not claimed by this native-only part.
