# M2 T5 S1 P9 Verification: VGA Graphics Controller

Focused tests cover all nine retained graphics-controller register classes,
their defined bit widths, indexed reads and writes, invalid indexes and widths,
reset, and machine composition. Video planes, latches, and browser rendering
remain the following native hardware work.
