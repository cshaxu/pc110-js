# M2 T5 S5 P1 Verification: COM1 UART Plan

The selected ROM trace identifies `0x3FA` as the next native hardware boundary.
The plan requires a complete UART implementation and focused port/IRQ tests
before later parts may claim ROM advancement.
